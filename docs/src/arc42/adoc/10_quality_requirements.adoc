[[section-quality-scenarios]]
== Quality Requirements

=== Quality Requirements Overview

The quality goals (Chapter 1) derive from the laws (Chapter 2):

[options="header"]
|===
| Attribute | Requirement
| *Integrity* | Every accepted byte is re-derived against its κ (Law L5).
| *Reproducibility* | A holospace's identity is the κ of its definition; identical inputs yield identical holospaces.
| *Portability* | The same holospace κ boots on any peer (browser / native / bare-metal).
| *Efficiency (memory)* | The store is the address space; content dedupes; RAM is a bounded cache (Law L3).
| *Autonomy (no server)* | No host, no account, no control plane; peers are content-addressed (Law L1).
| *Authority* | The documentation is the single authoritative source; features trace to it.
|===

=== Quality Scenarios

[options="header"]
|===
| # | Scenario | Expected response
| QS1 | The same git repo + `devcontainer.json` is provisioned twice, on different peers. | Both yield the same holospace κ.
| QS2 | A holospace is suspended on one signed-in instance and resumed on another. | It resumes from the κ snapshot; unchanged state dedupes.
| QS3 | A gateway (e.g. GitHub Pages, or a peer) returns bytes that do not match the requested κ. | The bytes are rejected on re-derivation; the fetch fails over to another source.
| QS4 | A holospace's total state exceeds available RAM. | It still boots; content is demand-paged by κ-resolve, evicted by garbage collection.
| QS5 | An operator signs in on a new instance. | Their holospaces and state are discoverable and synchronise over the substrate.
| QS6 | A contributor implements a feature. | The feature links to the documentation section it realizes; the V1–V8 pipeline passes.
| QS7 | An unmodified Linux devcontainer is provisioned and booted. | Its real operating system boots to userspace and runs its real binaries (a shell, `apt`); behaviour matches the Linux specification.
| QS8 | An operator edits a file and runs a terminal command in a holospace's workspace projection. | The edit and command publish as canonical events on the holospace's channels; the κ snapshot advances; the editor and file tree reflect the new content (read by κ).
| QS9 | An operator signs in to the Platform Manager and organizes their holospaces, performing lifecycle operations (provision · enter · suspend · resume · terminate). | The console authenticates a self-sovereign identity, lists and organizes the holospaces with status, and each operation runs through the real browser peer; the console holds no state of its own (the holospaces and roster are κ in the store).
| QS10 | An operator *enters* a holospace from the Platform Manager. | A new browser tab launches the VS Code experience — the real Monaco editor and xterm.js terminal, integrity-verified — over the running devcontainer: the terminal drives the booted OS, the editor and file tree read/edit content by κ.
| QS11 | The devcontainer boots its operating system. | A real distribution root filesystem mounts over a VirtIO block device from the κ-disk; the terminal runs the real binaries (a shell, the coreutils) — behaviour matching the Linux specification and QEMU.
| QS12 | The operator edits a file in the editor and reads it in the terminal. | The editor and the OS share the workspace filesystem over VirtIO-9p (9P2000.L); the edit (content by κ) is the same file the terminal reads — one content, no copy.
| QS13 | The devcontainer runs its `devcontainer.json` `features` / `postCreateCommand`. | The OS reaches the network over VirtIO-net and the package manager installs the real packages; the setup commands run, matching the Dev Container specification.
| QS14 | The operator uses the workspace's language intelligence and extensions. | The real VS Code workbench (κ-verified) speaks the Language Server and Debug Adapter protocols to tools running in the devcontainer OS, and runs extensions the `devcontainer.json` selects — the authentic Codespaces/Gitpod experience.
|===

=== Verification and Validation

holospaces is evaluated against *external authoritative specifications and standards* — never against itself (no self-reference). The V&V is *defined by this documentation* and is authoritative: each invariant below names the external authority it is validated against, the mechanism that enforces it, and the witness. Real validation artifacts are imported and pinned, with provenance recorded in `vv/PROVENANCE.md`; imported artifacts are themselves content-addressed and verified on import. The executable framework lives in `vv/` and is the single V&V entry point (`vv/run.sh`).

The framework has two tiers:

* *Specification conformance* (`CS`) — the holospaces specification (this documentation) validated against the standards it is authored under. Live on every build.
* *Component conformance* (`CC`) — each component validated against the external authority for its behavior. Each is specified here and witnessed by its suite against that authority, never by self-reference.

=== Conformance catalog

[options="header"]
|===
| ID | Invariant | External authority | Enforcement | Witness
| CS-1 | Architecture structure conforms to arc42 | https://arc42.org[arc42] template (pinned) | V1, V2 | every doc build (live)
| CS-2 | The C4 model is well-formed | Structurizr DSL | V3 | every doc build (live)
| CS-3 | Rendered docs are valid GitHub-flavoured Markdown | CommonMark / GFM; GitHub-markup | V4, V5 | every doc build (live)
| CS-4 | The conceptual model is valid OPM | OPM (ISO 19450) OPL grammar | V6 | every doc build (live)
| CS-5 | Each OPD agrees with its OPL | OPM (ISO 19450) bimodality | V7 | every doc build (live)
| CS-6 | The lifecycle covers the standard processes | ISO/IEC/IEEE 15288 | V8 | every doc build (live)
| CC-1 | κ-labels are correct content addresses | the σ-axis hash standards (BLAKE3; FIPS 180-4 SHA-2; FIPS 202 SHA-3; Keccak) via their reference implementations | byte-for-byte equality with the reference σ-axis; re-derivation (Law L5) | live (`vv/suites/cc1-kappa-addressing`)
| CC-2 | The browser `.holo` engine equals the native one | the native https://github.com/Hologram-Technologies/hologram[hologram] executor as oracle (identical `.holo` yields identical κ) | differential check | live (`vv/suites/cc2-holo-engine`)
| CC-3 | A peer's storage obeys the substrate contract | the https://github.com/Hologram-Technologies/hologram[hologram] substrate conformance battery (TCK) | run the imported battery | live (`vv/suites/cc3-substrate-tck`)
| CC-4 | A devcontainer holospace matches its source, including its declared customizations | the https://containers.dev[Dev Container] specification (validated against the published `devContainer.base.schema.json`) and the OCI image spec | a reproducible-κ check (Q4); and the config's *managed parameters* are parsed and honoured (schema-validated; malformed values refused): `customizations.vscode.extensions` (the **base extensions** holospaces installs into the workbench from the open gallery, as a Codespace does — `CC-19`; none bundled by default), `forwardPorts` (the ports to forward — `CC-21`), the lifecycle commands `onCreateCommand`/`postCreateCommand`/`postStartCommand`/… (run in the devcontainer OS so it is ready on entry — `CC-22`), `containerEnv`/`remoteEnv` (the environment — `containerEnv` set in the container, `remoteEnv` layered over it — `CC-23`), and `features` (Dev Container Features, each imported by κ and installed in the OS — `CC-25`) — the full Dev Container managed-parameter set, each *honoured*, never parsed-and-dropped | live (`vv/suites/cc4-devcontainer`)
| CC-5 | Wasm code modules are specification-valid | the https://webassembly.org[WebAssembly] specification | module validation | live (`vv/suites/cc5-wasm`)
| CC-6 | A κ-addressed Wasm code module runs on the execution surface, on every peer's engine | the https://webassembly.org[WebAssembly] specification (validity of the code module + its host-ABI surface) and the https://github.com/Hologram-Technologies/hologram[hologram] `ContainerRuntime` contract (it boots, suspends, resumes, and migrates) on both the native Wasmtime engine and the `wasmi` interpreter engine (the browser + bare-metal `ContainerEngine`) | surface validation + lifecycle on the real runtime, native and interpreter — the κ-addressed code-module surface the system emulator binds (ADR-008's contract, generalized by ADR-009) | live (`vv/suites/cc6-execution-surface`)
| CC-7 | The κ-disk preserves a real filesystem | a real on-disk filesystem image (the https://www.kernel.org[Linux] `ext4` / FAT format, `e2fsprogs` as the authority) | bytes written through the `KappaStore`-backed block device read back byte-identically (`debugfs` reads the real files out of the round-tripped image); the disk image's κ is reproducible. The κ-disk is a *read-through cache* over the store — RAM caching the canonical store (Law L3), a transient accelerator, not a second medium (Law L4): a repeated read of the same content is served from RAM and never re-hits the store, with byte-identical results and an unchanged disk κ. The **system emulator's `virtio-blk` is this κ-disk**: every devcontainer's sectors are content-addressed in an owned `KappaStore` (all-zero sectors sparse, identical sectors deduped), not an in-RAM image — so a real OS boots its rootfs *through the substrate* (witnessed: `CC-14`/`CC-22` boot from it; the snapshot reconstructs it faithfully, `CC-30`) | live (`vv/suites/cc7-kdisk`)
| CC-8 | Arbitrary code imported by κ runs over capability-scoped I/O | the https://github.com/Hologram-Technologies/hologram[hologram] driver-import + `ContainerRuntime` contract; a code module's output is the oracle (identical code + input yields identical κ) | import + verify-by-re-derivation, then run real `storage_put`/`get` bounded by the capability set — I/O outside the storage roots / over budget is denied by the real runtime | live (`vv/suites/cc8-import-run`)
| CC-9 | A real operating system boots and runs on the emulator | the https://www.kernel.org[Linux] kernel + the reference RISC-V machine `qemu-system-riscv64` as the differential oracle (the same OS image) | a real, unmodified Linux kernel boots to userspace on the emulator core (the emulator is the SBI firmware); a real binary's output (the userspace marker + `/proc/version`) matches QEMU byte-for-byte; the κ snapshot is reproducible across re-runs and peers | live (`vv/suites/cc9-emulator`)
| CC-10 | A devcontainer's OS image + repository ingest as κ content | the https://containers.dev[Dev Container] and the https://github.com/opencontainers/image-spec[OCI image] specifications | the image's blobs are walked (index → manifest → config + layers) and verified by re-derivation against their OCI `sha256` digests — an OCI content digest *is* a κ-label (Law L5); a multi-platform image index selects the manifest for the emulator's platform (`linux/riscv64`) — a missing match is an explicit error, never a silently wrong architecture — and the selected layers decompress whether `tar+gzip`, `tar+zstd`, or plain `tar`; the validated `devcontainer.json` (`CC-4`) binds to the ingested image into a reproducible source identity (same source ⇒ same holospace κ, Law L1); the ingested image content is held as `CC-7` κ-disk blocks. The booted behaviour of the image is the emulator's (`CC-9`) | live (`vv/suites/cc10-ingestion`)
| CC-11 | The workspace projection renders and drives a running holospace | the editor/terminal surface (file content, terminal I/O): the substrate store + reference hashes for the Editor/FS view, and a real running Linux terminal + `qemu-system-riscv64` as the differential oracle for the Terminal | the editor reads the environment's content by κ (verified by re-derivation) and an edit advances that κ; the operator's terminal commands publish as canonical events that advance the holospace's κ snapshot, and the rendered terminal is byte-identical to QEMU on the same image. The integrated terminal is a *real* terminal, not a line submitter: it feeds the OS console **raw bytes** (every keystroke, control bytes, escape sequences) so the guest tty echoes and *line-edits* them and **Ctrl-C raises SIGINT** in the foreground process — the deployed `DEVCONTAINER_INIT` execs the shell under `setsid -c` so it is a session leader with the console (`/dev/hvc0`) as its controlling terminal, giving the tty a foreground process group (without it Ctrl-C has nowhere to go). Output streams as a *delta* (only the newly-produced bytes per frame, O(new) not O(total)). Witnessed on the deployed devcontainer (`cc11_raw_terminal`: raw echo + a backspace-edited command + Ctrl-C interrupting `sleep 999999`) and in the browser (`terminal-test.mjs`) | live (`vv/suites/cc11-workspace`)
| CC-12 | The Platform Manager is a management console for the holospace lifecycle | a self-sovereign identity (`CC-1` σ-axis hashes) for sign-in, and the validated lifecycle paths the console composes — provision (`CC-4`), enter→boot (`CC-9`), and the workspace projection (`CC-11`) — exercised in a real browser | the operator signs in to a content-addressed identity and *launches a holospace from a git repository URL* (the Codespaces/Gitpod flow, not a pasted config): the page fetches the repository's `.devcontainer/devcontainer.json` over its host's raw surface and verifies it on receipt (Law L5), and a repository with none opens on the *usable default* image (`buildpack-deps` — `curl`/`git`; one source of truth with the host importer, `CC-20`). The `(repo, reference, config, arch)` tuple is the holospace identity (Law L1 — reproducible, repo/reference/architecture-distinct). The console lists and organizes the operator's holospaces with status, and each lifecycle action (provision · enter · suspend · resume · terminate) plus the per-guest settings (the `CC-28` operation classes) is driven through the real browser peer; the console holds no state of its own (Law L3 — the holospaces and roster are κ in the store) | the gated suite witnesses content-addressed identity + reproducible provisioning κ (`tests/cc12_manager_console.rs`); the browser lifecycle UX is witnessed by `scripts/browser-manager-test.sh` (CI `browser` job, `manager-test.mjs`) — live (`vv/suites/cc12-manager-console`)
| CC-13 | Entering a holospace renders the VS Code devcontainer experience | the **real VS Code editor and terminal components** — the https://microsoft.github.io/monaco-editor/[Monaco] editor and https://xtermjs.org[xterm.js] terminal (the libraries VS Code / Codespaces / Gitpod are built from), pinned and verified by re-derivation (Law L5) — plus the Dev Container spec (`CC-4`) and the running OS terminal (`CC-9`/`CC-11`) | in a real browser, the workspace tab loads the integrity-verified Monaco + xterm.js, boots the holospace's devcontainer OS on the emulator, and presents it as the IDE: the xterm.js terminal drives the running OS (output matches the `CC-11`/QEMU oracle), the Monaco editor + file tree read and edit environment content by κ, and the `devcontainer.json` validates against the spec (`CC-4`) | the gated suite witnesses the pinned Monaco + xterm.js re-deriving to their κ (`tests/cc13_vscode_workspace.rs`); the in-browser workspace UX is witnessed by `scripts/browser-manager-test.sh` (CI `browser` job, `workspace-test.mjs`) — live (`vv/suites/cc13-vscode-workspace`)
| CC-14 | The devcontainer boots a real OS root filesystem over a VirtIO block device | the OASIS https://docs.oasis-open.org/virtio/virtio/v1.2/virtio-v1.2.html[VirtIO] specification (v1.2 — `virtio-mmio` + `virtio-blk`) and the https://riscv.org/technical/specifications/[RISC-V] PLIC, a real OCI base image (`CC-10`) assembled into an ext4 `CC-7` κ-disk, and `qemu-system-riscv64` as the differential oracle | the emulator's PLIC + `virtio-mmio`/`virtio-blk` device obeys the VirtIO spec (the descriptor/available/used-ring protocol, the modern feature negotiation, the PLIC claim/complete handshake); a real, unmodified Linux kernel mounts the assembled rootfs from `/dev/vda` and runs real userspace, behaviour matching QEMU on the same kernel + rootfs; the boot is byte-for-byte reproducible (Law L1; ADR-011) | live (`vv/suites/cc14-virtio-block`)
| CC-15 | The editor and the running OS share the workspace filesystem | the VirtIO spec (`virtio-9p`) and the https://ericvh.github.io/9p-rfc/rfc9p2000.l[9P2000.L] protocol, with `qemu-system-riscv64` (its 9p server) as the differential oracle | the emulator serves the workspace's files over a spec-conformant `virtio-9p` device (the 9P2000.L message protocol — attach/walk/getattr/setattr/lopen/lcreate/read/write/readdir/mkdir/unlinkat/renameat); the OS mounts it over `virtio-9p` and reads/writes the same files holospaces holds — a file holospaces places on the share is read by the OS, and a file the OS writes is read back by holospaces (one content, Law L1). Mutations are *honoured*, not dropped: a guest `chmod` persists (`setattr` applies the mode) and `rm`/`mv` (and the editor's delete/rename/create-folder — the host-side duals, `CC-17`) take effect over `unlinkat`/`renameat`/`mkdir` (ADR-011) | live (`vv/suites/cc15-virtio-9p`)
| CC-16 | The devcontainer reaches the internet (git clone / apt / npm) | the VirtIO spec (`virtio-net`), the TCP/IP standards (DHCP, ARP, TCP), and `qemu-system-riscv64`'s user-mode network as the differential oracle | the emulator's spec-conformant `virtio-net` device feeds a *userspace TCP/IP NAT* (ARP + DHCP so the guest gets an address; the guest-facing TCP state machine); each guest flow is relayed out over a *pluggable egress transport* — a direct host socket on a native peer, a **WebSocket tunnel to a relay** on the browser peer (no raw NIC in a tab; ADR-014). The guest does DHCP, opens a TCP connection, and completes an HTTP exchange through the NAT, matching QEMU; a real package install runs over it (Dev Container spec, `CC-4`) | live (`vv/suites/cc16-network`)
| CC-17 | Entering a holospace opens the real VS Code workbench over a per-peer extension host | https://code.visualstudio.com[VS Code] (the real web workbench build + its `vscode.dev`/`github.dev` *web* extension-host model and, on server-capable peers, its remote-server protocol) and its published https://code.visualstudio.com/api/extension-guides/virtual-documents[`FileSystemProvider`] + terminal APIs, with the Dev Container spec (`CC-4`) | the workspace tab loads the κ-verified (Law L5) VS Code web workbench and presents the authentic UI (activity bar, command palette, settings, source control); the workbench drives an extension host *matched to the peer* (ADR-015): the **web model** on the *browser* peer (the extension host in the browser's web worker — the `vscode.dev`/`github.dev` model — which boots the holospace and binds the workbench to it; the *deployed* realization), and the **remote model** on a server-capable peer (the extension host in the devcontainer OS over VS Code's remote protocol). In both, the backends are the *same* holospace primitives — the `FileSystemProvider` is the `virtio-9p` workspace (`CC-15`) and the terminal is the holospace console (`CC-11`) — so parity holds (ADR-012/015) | Phase 1 live — the real workbench loads κ-verified (`vv/suites/cc17-vscode-workbench`); Phase 2 live — the editor-side filesystem over the shared `virtio-9p` workspace, content-addressed, a file the editor writes read by the OS over 9p (`vv/suites/cc17-workspace-fs`), *and* the real workbench is served that workspace the **supported way** — Microsoft's own `@vscode/test-web` built-in `FileSystemProvider` (the github.dev/Codespaces mechanism), its `readDirectory` reaching the real editor (`vv/suites/cc17-workbench-fs`, Chromium); **Phase 3 live — the real workbench bound to the *running holospace*, served statically (no server): holospaces is the substrate gateway that serves Microsoft's κ-verified executable core *byte-identical* (Law L5) and *composes* it as its own content with VS Code's supported web `create()` bootstrap + the `holospace-fs` builtin extension (the web extension-host model, ADR-015) — which boots the holospace *in the extension-host worker* (the browser is a first-class compute substrate) and binds the workbench to its `virtio-9p` workspace (`CC-15`, a FileSystemProvider) and console (`CC-11`, the integrated terminal), wired to the open gallery (Open VSX, `CC-19`). The real workbench loads κ-verified, the running holospace's workspace mounts into the editor, and a file the holospace holds is read back through the editor over the live workspace (`vv/suites/cc17-workbench-holospace`, Chromium). This is the *deployed* Workspace Projection (the Manager enters it; the `CC-13` Monaco workspace is the documented fallback)** — language servers / debug adapters in the OS (`CC-18`/`CC-19`) speak over this host
| CC-18 | The workbench provides language intelligence | the https://microsoft.github.io/language-server-protocol/[Language Server Protocol] specification (via `lsp-types` — rust-analyzer's own LSP type crate, the spec's authoritative Rust embodiment); a real language server (running in the devcontainer OS) as the authority | a real language server runs in the devcontainer OS (`CC-14`) under the remote extension host (ADR-015) and the workbench speaks LSP to it over the standard stdio base-protocol transport; the session (`initialize`/`didOpen`/`hover`/`completion`/`definition`) is built with `lsp-types` and the server's responses are validated back through `lsp-types` — advertised hover/completion/definition capabilities, a hover for the symbol, completions, a go-to-definition `Location`, and `TODO` diagnostics for a real source file all conform to the LSP spec (ADR-012). holospaces' own emulator runs the server under a real *libc* and the responses appear on its console. The *deployed* delivery is witnessed too: the SAME server runs as a TCP service in the OS (`lsp-demo --listen`) and a full LSP session flows to it over the `CC-33` in-process substrate bridge (ADR-020) — every response (capabilities, hover, completion, definition, diagnostics) conforms to the LSP spec through `lsp-types` — so the browser workbench gets real language intelligence from a server in the devcontainer OS with **no Node** (the browser-tab realization of the VS Code remote model, ADR-015) | live (`vv/suites/cc18-lsp` — stdio + the bridge witness `cc18_lsp_bridge`)
| CC-19 | The workbench runs extensions and their integrations, and debugs | the VS Code https://code.visualstudio.com/api[extension API] + its https://code.visualstudio.com/api/references/vscode-api#authentication[authentication provider] contract (the general integration mechanism), the https://microsoft.github.io/debug-adapter-protocol/[Debug Adapter Protocol] specification, and — as one concrete integration witness — the https://docs.github.com/en/rest[GitHub REST API] + https://docs.github.com/en/apps/oauth-apps[OAuth] | *arbitrary* extensions install from the open marketplace (https://open-vsx.org[Open VSX] — the gallery VS Code web uses, since the MS Marketplace ToS forbids non-MS products) and function; holospaces bundles *no* third-party extension by default (no lock-in). An installed extension runs in the extension host and reaches its backing services through the devcontainer's own filesystem (`CC-15`), terminal (`CC-11`), and network (`CC-16`) — *any* extension's published authentication-provider + API works the same way (auth is the user's, over the network — `CC-24`, ADR-017); the operator chooses any extension (the GitHub Pull Requests extension is one such *user* choice, never a holospaces default); and a debug session drives a real program through a DAP adapter running in the OS, conforming to the DAP spec | live (extensions) — the real workbench holospaces serves is *wired to the open gallery* (its `productConfiguration.extensionsGallery.serviceUrl` is Open VSX, **not** the Microsoft Marketplace — witnessed in `vscode-workbench-test.mjs`), so an operator installs *arbitrary* extensions, and a real extension *activates in the real workbench's extension host* the supported way (`@vscode/test-web --extensionDevelopmentPath`, its status-bar contribution appearing in the genuine workbench; `vv/suites/cc19-extensions`). The marketplace **displays and lists real extensions** from Open VSX — witnessed for real (`marketplace-test.mjs`: the gallery `extensionquery` returns and renders extensions, with their icons fetched from Open VSX), not merely that the serviceUrl string is wired — the regression guard for "the marketplace stopped displaying". A `devcontainer.json`'s `customizations.vscode.extensions` set (parsed by `CC-4`) is carried to the workbench on launch (as `additionalBuiltinExtensions` gallery ids, the vscode.dev / Codespaces pre-install mechanism), so the **web-capable ones auto-install from the open gallery** — *devcontainer configs install their needed dependencies* (witnessed `extensions-test.mjs`: a declared web extension is fetched from Open VSX and installed). *The web/remote boundary is explicit (ADR-015), never a silent "unsupported":* the **browser peer** runs VS Code's *web* extension host, so a *web extension* (a browser entrypoint) installs and runs; a *workspace/Node extension* (the marketplace's compilers, debuggers, and the Node-hosted language extensions) needs the *remote* extension host running in the OS. **holospaces *is* the remote** the workbench talks to (`CC-34`), in the tab on the substrate — there is no separate vscode-server and no other deployment. It delivers the remote model in two parts (ADR-020): the **server-backed editor capabilities** — language intelligence (`CC-18`) and debugging — run *now* in the browser tab, a real server inside the booted devcontainer reached over the `CC-33` in-process bridge with **no Node**; the **extension host** that activates *arbitrary* marketplace extensions is holospaces' own, on the *hologram substrate* — the substrate is used *instead of* the legacy Node `vscode-server` (as it replaces the cloud VM), its API surface backed by the holospace's own filesystem/terminal/network. That host is *now live (`CC-48`)*: a *Node-only* extension (a `main`, no `browser` entrypoint) installs from Open VSX and *runs `activate()` in the substrate-native (wasm-exec) ext host* — on the browser peer's own JS engine, no Node on the host, no server — so the "not available for the Web" notice is *retired* for Node-only extensions (it stood, honestly not silently, only while `CC-48` was a target)
| CC-20 | A devcontainer provisions from a repository URL over the internet | the https://github.com/opencontainers/distribution-spec[OCI distribution specification] (registry pull — manifest + blobs by digest), an HTTP(S) git-host archive (the repository content at a reference), and the https://containers.dev[Dev Container] spec's default (a repository with no devcontainer) — the internet is an *untrusted gateway* (ADR-013) | holospaces fetches a repository's content by URL+reference and pulls the devcontainer's OCI image from its registry, *verifying every byte by re-derivation* (a registry digest *is* a κ; Law L5 — a tampered byte is refused); a repository with no `devcontainer.json` provisions on the *usable default* image (`buildpack-deps` — `curl`/`git`, so any repo opens functional); the imported content boots as a holospace (`CC-4`→`CC-10`→`CC-7`→`CC-14`). holospaces boots *arbitrary* real devcontainers, not a fixed demo: *two distinct real OCI images*, each declared by its own repository's devcontainer.json, are each pulled + assembled + *booted to a real Linux* — so the launched holospace is the repository's actual devcontainer, whatever it declares. The imported content then travels the **substrate's own content-addressed transport** to a peer that did *not* import it: the importer serves its store as an *untrusted* HTTP-CAS gateway (`hologram-net-http`, the `/cas/{κ}` protocol) and a second peer with no local content fetches the rootfs + kernel by κ through `get_with_fetch`, *verifying each on receipt* (Law L5) before booting — the exact path the **browser peer** takes to boot a devcontainer it did not assemble locally (its `fetch()` is the same `/cas` client; verify happens in wasm), so the boot content is delivered trustlessly from a generic hologram gateway, no bespoke server. Witnessed *hermetically* (a localhost server serving pinned content — reproducible), through the *substrate gateway* (the cross-peer fetch-and-boot), and against the *live* internet (a network-gated smoke test) | live (`vv/suites/cc20-import`)
| CC-21 | A server running in the devcontainer is reachable as a forwarded port (the running-app preview) | the OASIS https://docs.oasis-open.org/virtio/virtio/v1.2/virtio-v1.2.html[VirtIO] spec (`virtio-net`) + the TCP/IP standards, with `qemu-system-riscv64`'s user-mode `hostfwd` as the differential oracle; the Codespaces/Gitpod *port-forwarding / preview* capability | the userspace TCP/IP NAT accepts an *inbound* connection on a forwarded port and bridges it to a listener inside the OS (the dual of the `CC-16` egress; the *ingress* seam — a direct host listener on a native peer, a relay-served route on the browser peer, ADR-016). A real HTTP server started in the devcontainer is fetched from outside the OS, byte-identically, matching QEMU `hostfwd` — the dev server / app preview a Codespace surfaces in its Ports view | live (`vv/suites/cc21-port-forward`)
| CC-22 | The devcontainer's lifecycle commands run on create | the https://containers.dev[Dev Container] specification's lifecycle hooks (`onCreateCommand`, `updateContentCommand`, `postCreateCommand`, `postStartCommand`, `postAttachCommand`) and their run order + the `ext4` on-disk format (e2fsprogs `e2fsck`/`debugfs` as the oracle), with `qemu-system-riscv64` as the differential runtime oracle | the Boot Orchestrator parses the lifecycle commands from `devcontainer.json` (`CC-4`), builds an `/init` lifecycle runner from the parsed config (the commands in spec order, with the config's `remoteEnv` applied, each framed with its spec hook name), and injects it into the assembled rootfs (`CC-14`) over a base image that provides a shell — so the booted OS runs the declared commands in spec order and the environment is *ready on entry*, as in a Codespace/Gitpod. The injected `/init` reads back byte-identically from the `ext4` (e2fsprogs); holospaces' own emulator boots the rootfs and runs the declared `postCreateCommand` under a real *libc* (`busybox`) shell with `remoteEnv` applied, and `qemu-system-riscv64` produces the same markers on the byte-identical rootfs (the differential oracle) | live (`vv/suites/cc22-lifecycle`)
| CC-23 | The workspace is personalized — settings, dotfiles, and secrets | the https://containers.dev[Dev Container] spec (`remoteEnv`/`containerEnv`, `secrets`), the VS Code settings + https://code.visualstudio.com/docs/editor/settings-sync[Settings Sync] surfaces, and the Codespaces/Gitpod *dotfiles* convention | the operator's settings, dotfiles, and secrets are κ-addressed content scoped to their identity (`CC-1`/`CC-12`, Law L3 — held in the store, synced by `KappaSync`, not a server account; a `Personalization` is a hologram `Realization` embedding the operator identity, so the *same* settings/dotfiles/secrets under a *different* operator are a *different* κ); on entry holospaces applies them — settings to the workbench, dotfiles + `remoteEnv`/secrets into the devcontainer OS (`CC-14`/`CC-15`) — so the operator's environment follows them across peers (Q6), as a Codespace/Gitpod carries dotfiles + secrets. holospaces' own emulator boots the assembled rootfs and applies the personalization under a real *libc* (`busybox`) shell — the secret is present in the OS environment (confirmed, not leaked) and the operator's dotfile is in the home directory (e2fsprogs verifies both the dotfiles and the entry `/init` are exact in the rootfs) | live (`vv/suites/cc23-personalization`)
| CC-24 | The devcontainer signs in to GitHub and other services over the network | the https://datatracker.ietf.org/doc/html/rfc8628[OAuth 2.0 Device Authorization Grant (RFC 8628)] — the secretless flow `gh auth login` uses and that fits a Pages deployment — with a hermetic GitHub-shaped device-flow server as the differential oracle | holospaces does *not* intermediate auth (ADR-017): it provides the network (`CC-16`) and the devcontainer's tools (and any extension's authentication provider) sign in over it with the *service's own* OAuth flow; the devcontainer requests a device + user code, polls through `authorization_pending`, and obtains an access token — **the token lives in the devcontainer, never in holospaces** (content-blind; Laws L1/L3). Service-agnostic (GitHub, GitLab, a registry, a cloud SDK — each with its own provider) | live (`vv/suites/cc24-auth`)
| CC-25 | The devcontainer's Dev Container Features install on create | the https://containers.dev/implementors/features[Dev Container Features] specification (the feature artifact format — an OCI artifact carrying `devcontainer-feature.json` + `install.sh` — and the install contract: `install.sh` runs in the container with the declared options as environment, before the lifecycle commands) and the `ext4` on-disk format (e2fsprogs as the oracle), with `qemu-system-riscv64` as the differential runtime oracle | the Boot Orchestrator parses the `features` a `devcontainer.json` declares (`CC-4`), **imports each feature artifact by κ** (verify-by-re-derivation — the `CC-20` machinery, *no notional functionality*) and **places it into the rootfs**, and the generated `/init` runs each feature's `install.sh` *before* the lifecycle commands (`CC-22`), with the declared options passed as *uppercased environment* — so feature-provided tools are present on entry, exactly as a Codespace installs features (ADR-016). Not parse-and-drop: the declared feature is *applied* | live (`vv/suites/cc25-features`)
| CC-26 | A devcontainer declared with a Dockerfile build is built on the substrate | the https://docs.docker.com/reference/dockerfile/[Dockerfile reference] (the instruction semantics — `FROM`/`ARG`/`ENV`/`WORKDIR`/`COPY`/`RUN`; line continuations; variable substitution) and the Dev Container spec's `build`, with the `ext4` format (e2fsprogs) and `qemu-system-riscv64` as the differential oracle | a `devcontainer.json` may declare its container as a *Dockerfile build* (`"build": { "dockerfile": … }`) instead of a prebuilt `image`; holospaces parses it (`CC-4`, retained as `ImageSource::Build`, *not dropped*) and honours it the substrate-native way — *no Docker daemon*: the Dockerfile's `FROM` image is pulled + assembled as the base rootfs (`CC-20`/`CC-10`), the `COPY` sources from the build context are injected into the rootfs, and the `RUN` instructions run **in the devcontainer OS** during the build phase with the build `ARG`s/`ENV`s in scope (`CC-22`/`CC-25` machinery) — the built rootfs is the result. An unknown Dockerfile instruction is an explicit error, never a silent drop | live (`vv/suites/cc26-build`)
| CC-27 | A devcontainer declared with a Docker Compose file resolves its service | the https://compose-spec.io[Compose specification] (the `services.<name>.{image\|build}` model) and the Dev Container spec's `dockerComposeFile` + `service` | a `devcontainer.json` may declare its container with a `dockerComposeFile` and a `service` (the service that *is* the devcontainer); holospaces parses it (`CC-4`, retained as `ImageSource::Compose`, *not dropped*), reads the compose file from the repository, and resolves the named service's declared image source — its `image` (then pulled via `CC-20`) or its `build` (then a Dockerfile build via `CC-26`). Provisioning follows those paths; the gated witness exercises the parse + resolve (the routing code exists but is not driven end-to-end — no blob is pulled, built, or booted). A missing or ambiguous service is an explicit error, never a silent default; multi-service orchestration is out of scope for a single devcontainer | live (`vv/suites/cc27-compose`)
| CC-28 | The control plane reconfigures a running instance over the substrate | the substrate's content-addressing + sync contract (a real loopback HTTP content-addressed gateway, *verify-on-receipt* — Law L5, the model the `e2e` roster-sync witness uses) and, for the live network directive, a real host `TcpListener` on the running machine (the `CC-21` ingress dual); the Codespaces/Gitpod (Ona) control-panel *reconfigure* UX | the control panel reconfigures a *running* instance across the four operation classes — *lifecycle / storage / network / account-user* — UOR-native (ADR-018): the panel builds a κ-addressed `Configuration` (a hologram `Realization` embedding the issuing operator identity and the target instance κ, plus ordered directives), `Manager::configure` stores it (Law L2) and records it as the instance's current configuration, and the running instance *resolves* it over the substrate (verify-by-re-derivation, Law L5), checks it targets *this* instance and the operator is authorized, and *applies* it. The control plane **actually manages** the instance: `Manager::reconfigure` publishes the configuration *and drives the running session with it* — a lifecycle directive really transitions it (suspend → κ snapshot, resume, terminate), and a storage/network directive replaces the effective capability set (a new κ, persisted). A `forwardPort` directive forwards a port on the *running* machine (`Machine::forward_port`, a real reachable listener bound live — no reboot). In the browser, the panel carries the configuration to the running instance tab over a `BroadcastChannel` control channel, which verifies it (L5) and applies it (`Workspace::reconfigure`). Reproducible (same configuration ⇒ same κ, Law L1) and authority-scoped (a configuration for a *different* instance, or from an *unauthorized* operator, is refused — Law L3). No server, no control-plane→instance RPC; the configuration is content, the substrate carries it | live (`vv/suites/cc28-reconfigure`)
| CC-29 | L5 verification is placed at the trust boundary, not charged on every local read | the substrate's own `verify_kappa` re-derivation contract — the exact primitive `get_with_fetch` (the loopback HTTP-CAS gateway, *verify-on-receipt*) and the OCI ingestor (`verify_kappa_axis`, `CC-10`) run where untrusted bytes enter; the threat modelled by a *forging store* that serves bytes which do not re-derive to the requested κ | the deployed Pages artifact is a *production target*: V&V proves Law L5 exhaustively in CI, so the deployed peer is not charged to re-prove it at runtime (ADR-019). A `ReadVerify` policy makes the placement explicit — `OnRead` re-derives κ (the default for a general/boundary caller, and the mode the conformance suite drives over *all* content) versus `Trusted`, which returns the canonical store's bytes as-is because the `KappaStore` *is* the memory and RAM is its cache (Law L3); the in-session browser peer reads locally with `Trusted` (`Console::resolve`). The receipt boundaries are unchanged and still verify every untrusted byte. Witnessed: the boundary read (`OnRead`) rejects a forging store's liar; the trusted read returns the store's bytes without re-deriving; honest content reads identically under both policies; `resolve_local` defaults to the verifying boundary policy | live (`vv/suites/cc29-read-verify-boundary`)
| CC-30 | A suspended machine resumes from its κ snapshot | the snapshot's own determinism (Law L1) and the reference RISC-V machine `qemu-system-riscv64` as the differential oracle | `Emulator::restore` is the exact inverse of `Emulator::snapshot` (ADR-009's *running state as a κ snapshot* property made whole), so suspend → resume is a round trip: `restore(snapshot(m))` re-snapshots to the *same* bytes and κ — including a machine with a virtio-blk disk — and the resumed machine continues execution *byte-identically* (same console, same final κ). Resume is κ-addressed content, so a suspended machine migrates by its κ alone (Law L1); a truncated snapshot is rejected, never half-restored. The strong witness: a real Linux machine *suspended mid-boot* and resumed from its κ reaches the byte-identical userspace the un-suspended machine did. This is the substrate primitive a second launch resumes from instead of cold-booting | live (`vv/suites/cc30-resume`)
| CC-31 | A devcontainer resumes from a persisted κ snapshot over OPFS, in the browser | the host `CC-30` differential-resume witness + the substrate's own `verify_kappa` re-derivation contract, in a real browser (Chromium via Playwright) — the same wasm GitHub Pages serves | the browser peer *suspends* the running devcontainer to a κ snapshot (`Workspace::suspend`), gzips it — most of guest RAM is zero, so a ~512 MiB machine compresses to a few MiB — and persists it (with its κ) to the Origin Private File System. Across a real page *reload* (a brand-new runtime; only OPFS survives), it reads the snapshot back, *verifies it by re-derivation* (Law L5 — OPFS is durable but untrusted storage, ADR-019; a tampered snapshot is refused and it cold-boots), and *resumes* the machine (`Workspace::resume_devcontainer`) — no kernel fetch, no rootfs assembly, no cold boot. Witnessed: the snapshot gzips to a small fraction of the machine; after reload it re-derives to its recorded κ; the *workspace files are intact*; the resumed machine is *live* and continues the very boot it was suspended partway through, to userspace; a tampered snapshot is rejected. The machine snapshot is *κ-pure*: it does not carry the console *output* buffer (a projection of the past, not future-affecting machine state), so a freshly restored machine's console is empty — the terminal layer, not the machine, restores the visible scrollback (persisted beside the snapshot and replayed on resume), so a resume at the *idle shell* (the steady state the periodic snapshot captures) comes back to the prompt you left, not a blank screen | live (`resume-test.mjs`; the host idle-shell resume witness `cc31_resume_terminal`)
| CC-32 | Distinct holospaces never bleed: each holospace's durable resume state is keyed by its identity (κ) | a deterministic keying witness that drives the real `build-workbench` config wiring and the real `holospace-fs` keying helpers (the deployed artifacts) | OPFS is per-origin, shared by every workbench tab, so a *fixed* snapshot filename would make every holospace read and write the *same* slot — launching one holospace would resume another's machine, and a removed/renamed holospace's remnants would bleed into the next launch. Identity is what-not-where (Law L1): the Platform Manager opens `workbench.html?id=<κ>`, `build-workbench` carries that κ into the workspace folder *authority* (`holospace://<κ>/workspace`), and `holospace-fs` keys every durable artifact (the resume snapshot, its κ, the scrollback) by it — so the *same* holospace resumes its own state across sessions while *distinct* holospaces map to *disjoint* OPFS slots and can never collide. Removing or terminating a holospace clears exactly its slot (the Manager's cleanup mapping is held identical to the workbench's, witnessed). Witnessed: two distinct κ launch URLs resolve, end-to-end, to disjoint snapshot files; an empty identity is the lone `default` slot; the Manager's delete-key matches the workbench's | live (`snapshot-keying-test.mjs`, the browser conformance job)
| CC-33 | A process inside the booted devcontainer is reachable from the workbench over an in-process substrate bridge | TCP and the guest's own listening socket, over the emulator's userspace NAT + ingress (`CC-16`/`CC-21`); the `qemu-system-riscv64` slirp network as the differential oracle for the NAT path | the browser workbench's extension host and the system emulator are one process (ADR-020), so reaching a server *inside* the guest is an in-process ingress connection into the emulator's own TCP stack, not a network round trip: the `Workspace` peer exposes `dial`/`send`/`recv`/`close` to a guest port, writes the client's bytes into the guest socket and reads the guest's bytes back, pumping the emulator between turns — the inward, in-process dual of the egress relay (ADR-014), with no relay, socket, or server (Law L4/ADR-001). The transport is peer-agnostic (Q6): the same devcontainer κ exposes the same listener through this bridge in the browser and through a real host socket on a native peer. Witnessed: a devcontainer boots a guest TCP listener (its `/init` starts a real server), the host bridge dials it and a byte stream round-trips faithfully both directions; a closed connection tears down; the bytes the guest received equal the bytes sent and vice-versa | live (`vv/suites/cc33-guest-bridge`)
| CC-34 | holospaces *is* the VS Code remote server (in the tab, on the substrate); it backs the editor over the bridge | the VS Code https://github.com/microsoft/vscode/blob/main/src/vs/platform/remote/common/remoteAgentConnection.ts[remote-server protocol] (the management + extension-host connection a Codespace's browser uses to its remote) and the VS Code https://code.visualstudio.com/api[extension API] | **holospaces is the remote** the workbench talks to — *not* a separate vscode-server on a separate machine. holospaces runs entirely in the GitHub-Pages browser tab on the substrate (the wasm peer boots the devcontainer *in the tab*); there is *no server and no other deployment* (ADR-001/ADR-005, Laws L1/L3/L4), and the `CC-33` in-process bridge (ADR-020) is the connection the workbench's remote talks over — the inward dual of the egress relay, not a socket to anywhere. Over that connection holospaces serves the remote's backends from the *same* holospace primitives a Codespace's server would: the filesystem (`CC-15`), the terminal (`CC-11`), and the *server-backed editor capabilities* — language intelligence (`CC-18`) and debugging — from a real server *inside the booted devcontainer*, **no Node, nothing outside the holospace** (`CC-18` is the witnessed exemplar: real hovers/completion/definition/diagnostics from an in-OS LSP server over the bridge; a heavier real server — rust-analyzer, clangd — is the *same* bridge, a larger binary). The **extension host** that activates *arbitrary* marketplace extensions (removing VS Code's "not available for the Web" notice) is *now live too* (`CC-48`). holospaces provides that ext host *on the hologram substrate* — replacing the legacy Node `vscode-server` exactly as it replaces the cloud VM (the substrate is used *instead of* the legacy machinery, ADR-008/009, Law L4): a substrate-native runtime for the ext host (a JS/Node-API runtime on the browser peer's *own* JS engine, native exec — not the emulated guest) with the VS Code + Node API surface backed by the holospace's *own* primitives — its filesystem (`CC-15`), terminal/process surface (`CC-11`), and network (`CC-16`) — **never** a legacy server or a separate deployment, of which there are none. Witnessed: holospaces serving the server-backed capability to the workbench over the bridge (`CC-18`/`CC-33`, live) and a Node-only marketplace extension activating in the substrate-native ext host (`CC-48`, live) | live (holospaces serves the server-backed capability: `cc18-lsp` + `lsp-test.mjs`); the substrate-native extension host is *now live too* (`CC-48`: a Node-only Open VSX extension activates in the wasm-exec ext host — `vv/suites/cc48-ext-host`; no server, no other deployment)
| CC-35 | The system emulator executes the AArch64 (A64) integer ISA correctly | the **Arm Architecture Reference Manual** (ARM DDI 0487) for the A64 base instruction set + `PSTATE.NZCV` condition flags, with **`qemu-system-aarch64`** as the differential oracle | the emulator decodes and executes the A64 integer core — data-processing (immediate + shifted/extended register: `ADD`/`SUB`/`ADDS`/`SUBS`, the logical group, `MOVZ`/`MOVN`/`MOVK`, bitfield/`ADR`/`ADRP`), multiply (`MADD`/`MSUB`/`MUL`), conditional select, the full load/store family (`LDR`/`STR`/`LDP`/`STP`, the addressing modes + sign/zero extension), and control flow (`B`/`BL`/`B.cond`/`CBZ`/`CBNZ`/`TBZ`/`RET`/`BR`/`BLR`) with the `NZCV` flags driving condition codes — on a flat little-endian RAM with 64-bit `X0`–`X30`/`SP`. Witnessed against hand-assembled A64 batteries whose final register/memory state is the Arm-ARM-defined result and matches `qemu-system-aarch64` byte-for-byte (`X0` carries the self-check verdict) | live (`cc35-aarch64-core`) — the A64 integer core; the `qemu-aarch64` differential PASSes (real toolchain-assembled batteries)
| CC-36 | A real arm64 Linux kernel boots to userspace on the emulator | a real, unmodified **`arm64` Linux** kernel (the most stringent A64 correctness test) over the **ARM `virt` platform** (the `GICv2/v3` interrupt controller, the ARM generic timer, `PSCI` for boot/reset, the devicetree the kernel expects) — the AArch64 VMSAv8-64 translation system (ARM DDI 0487) — with **`qemu-system-aarch64 -M virt`** as the differential oracle | the emulator adds the EL0–EL1 exception model (vectors, `ESR`/`ELR`/`SPSR`, `SVC`/`ERET`), VMSAv8-64 paging (`TTBR0/1`, the 4 KiB-granule walk with access/dirty), and the *virt* platform devices (`GIC` distributor/CPU/redistributor, the `CNTV` timer, a `PSCI` SMC interface for `CPU_ON`/`SYSTEM_OFF`), reusing the `virtio-mmio` block/9p/net devices unchanged. A real `arm64` Linux boots over it, mounts a `virtio-blk` rootfs, reaches `Run /init`, and PID 1 prints its marker + `/proc/version`, byte-identical to `qemu-system-aarch64` booting the same image | live (`cc36-aarch64-linux`) — a real, unmodified `arm64` Linux 6.6 boots to userspace on the emulator (EL0/EL1 + VMSAv8-64 MMU + GICv2 + generic timer + PSCI + PL011), PID 1 prints its marker + `/proc/version` and powers off via PSCI; the `qemu-system-aarch64 -M virt` differential PASSes byte-for-byte
| CC-37 | An arm64 devcontainer runs the ecosystem's stock `linux-arm64` binaries | the https://containers.dev[Dev Container] + OCI image specs over an `arm64/linux` image, and the stock upstream `linux-arm64` builds (Node, a language server, `openvscode-server`) as the authority — *no per-ISA porting* | an `arm64` holospace boots (`CC-36`) from a κ-addressed `arm64` image (`CC-20`/`CC-9`) over the shared `virtio-blk` κ-disk (the shared `emulator::devbus`, Law L4 — the 9p workspace, network, and bridge reach the AArch64 core via `CC-46`), and runs the ecosystem's *unmodified* `linux-arm64` binaries with no riscv64 workaround. (The extension host that activates *arbitrary* marketplace extensions is `CC-48` — now *live*, realized as native hologram exec on the browser peer's own JS engine, _not_ an in-guest `openvscode-server`; the "interpreter wall" approach was rejected.) Witnessed: a stock `arm64` binary runs in the OS (the 9p/network/bridge arch parity is `CC-46`) | live (`cc37-aarch64-devcontainer`) — an `arm64` devcontainer boots from its κ-disk `virtio-blk` rootfs (the **shared** substrate device bus, `emulator::devbus`, that the RISC-V machine uses — no per-ISA re-implementation) and runs the **stock, unmodified `linux-arm64` glibc busybox** as PID 1 — executing its baseline NEON `memcpy`/`strlen` (Advanced SIMD is mandatory on AArch64), forking children that fault copy-on-write pages, and dispatching **unmodified applets** over fork+exec + command substitution (`uname -m` → `aarch64`, a shell sum `1..1000 == 500500`, `head` reading the real `/proc/version`), powering off via PSCI — byte-identical to `qemu-system-aarch64 -M virt`. The **Advanced-SIMD (NEON) ISA frontier is closed**: the emulator implements the full A64 integer ISA + Advanced SIMD + scalar FP + `DC ZVA` + VMSAv8-64 write-permission (copy-on-write / dirty) enforcement, so **arbitrary** `linux-arm64` glibc programs run with no riscv64 workaround. The heavier `linux-arm64` glibc binaries run on the same complete ISA. (CC-34's substrate-native extension host — the one that activates arbitrary marketplace extensions — is *live* as `CC-48`, realized as native wasm exec on the browser peer, not an in-guest server.)
| CC-38 | A browser peer and a bare-metal peer exchange content over the uor-native network ("the browser as a router") | the substrate's content-addressed network — `hologram-net-bare`'s `BareNetSync` (the uor-native `fetch`/`announce`/`discover` frame protocol, **verify-on-receipt** — SPINE-4) over the `NetworkInterface` HAL — reused, not reinvented (ADR-006); no central operator, no client/server roles | a peer reaches another peer's content the same content-addressed way on every surface: holospaces supplies a *portable* `NetworkInterface` (`content_net::PacketLink`) so the **same** `BareNetSync` drives the content network in a browser tab (wasm), on a bare-metal board (`thumbv7em-none-eabi`), and on a native host. A browser peer and a bare-metal peer therefore **interoperate by construction** — they are the *identical* implementation over the *identical* frame codec, not a shared adapter. A peer with no local content fetches a κ from a linked peer, the bytes are **verified by re-derivation before they are accepted** (a forging responder is rejected), and a κ no peer holds resolves to nothing (no forging, no false content); the network is symmetric (either direction). The transport that carries a link's frames is surface-specific — a WebRTC data channel between browser tabs (a browser feature), a real NIC on bare metal; the browser transport is now realized (`CC-49`: two browser peers exchange κ-content over a real `RTCDataChannel`, no server), and the protocol above it is proven | live (`vv/suites/cc38-content-net`) — the cross-peer exchange (`crates/holospaces/tests/cc38_content_net.rs`, two peers exchange content + verify-on-receipt, an unheld κ is absent) plus the **compatibility gate** (the identical `content_net` compiles for `thumbv7em-none-eabi` *and* `wasm32`, so the implementations cannot diverge); the browser surface drives the same path in Chromium (`holospaces-web::Console::content_network_selftest`, in the manager test)
| CC-39 | A holospaces node is the egress exit a browser tab routes through | the browser's own egress framing (`WsEgress` OPEN/DATA/CLOSE, `CC-16`) and a real TCP host as the differential — the node is a flashable *bare-metal peer you own*, the mesh's exit, not a bespoke external proxy | a browser tab has no NIC, so its guest's arbitrary internet (`apt`/`pip`/`npm`, a `git` clone, an outbound socket) leaves the tab through a holospaces node — a low-powered device running the server side of the protocol the browser *already* speaks. The node forwards each guest TCP connection to the real internet and frames the host's replies back; an unreachable host reports `FAILED`, never a silent drop. holospaces keeps the devcontainer's *compute* in the tab (Codespaces-replacing); the node supplies the network the sandbox cannot. The *same node* fills three roles a tab cannot: *egress* (forward the guest's TCP to the internet, content-blind — SEC-7), *storage-sync* (a persistent file-backed `KappaStore` served over HTTP-CAS, `GET /cas/{κ}`, so the operator's content survives reloads/devices, verified on receipt — `CC-38`), and *OTA* (fetch its own κ-addressed update from the Pages site, verify it re-derives — Law L5 — and stage it; a forged update is refused) | live (`vv/suites/cc39-node-egress`) — egress end-to-end over the **real WebSocket** the browser uses (`crates/holospaces-node/tests/ws_egress.rs`) + content-blind forwarding; storage-sync (content persists across a restart and serves over HTTP-CAS, verified on receipt); OTA (a κ-addressed update is fetched, verified, and staged — a forged update refused)
| CC-40 | Product security: the threat model's properties are enforced, not asserted | the Product Security & Threat Model (chapter 13) — the UOR-native security primitives (content-addressed integrity, frame-relative confidentiality, object-capability authority, self-sovereign identity, dense-matrix dedup cost) as the authority | each security requirement is *enforced by construction* and *witnessed in code*, so a property cannot silently regress: *SEC-1 integrity* — a tampered byte fails re-derivation and the content network never fabricates content for an unheld κ (Law L5); *SEC-2 authority* — `Capabilities::admits` refuses every escalation vector (an unheld flag, a wider quota, an unbounded budget under a bounded parent, a foreign storage root), authority only attenuates; *SEC-3 cost/dedup* — identical content resolves to one κ, stored once (the observable form of the dense-matrix cost model whose decreasing per-node floor is a substrate property); *SEC-4 identity* — self-sovereign, deterministic, unforgeable; *SEC-5 confidentiality* — the κ is the capability to perceive, an unknown κ is absent (the holospaces-observable form of frame-relative perception, whose full realization is the Prism layer); *SEC-6 reference resolution* — content is verified by re-derivation against the κ on its own axis (an OCI digest _is_ a κ), so a tampered blob is refused regardless of where the reference led; *SEC-7 egress boundary* — the exit is content-blind, forwarding opaque bytes it cannot perceive (`CC-39`). An adversary→defense matrix and a requirement→witness traceability table make the coverage explicit; the legacy-internet egress boundary is named precisely (old-world properties on the outside leg only), not wished away | live (`vv/suites/cc40-product-security`, `crates/holospaces/tests/cc40_product_security.rs`)
| CC-41 | A Chromebook guest reaches the internet via a local Chrome extension (no node, no proxy) | the browser's own egress framing (`WsEgress` OPEN/DATA/CLOSE, `CC-16`) over the *Direct Sockets API* (`TCPSocket`) and a real TCP host as the differential — the third egress surface, the one that makes a tab *self-contained* | a browser tab cannot open a raw socket, but an MV3 service worker can (Direct Sockets). The holospaces egress extension (`crates/holospaces-web/extension/`) is a *local egress node in the browser*: it speaks the *same* protocol the browser uses for a node (`CC-39`) — only each guest connection is a `TCPSocket` it opens — so a Chromebook's devcontainer gets arbitrary internet with no node, relay, or proxy. The extension is *minimal by design* for a fast store review (no host permissions, no tabs, no storage — only sockets + the operator's one origin in `externally_connectable`) and *content-blind* (SEC-7). Direct Sockets is a gated capability (enabled out of band, an enterprise policy or `chrome://flags`), so the worker's behaviour is witnessed *hermetically* with `TCPSocket` polyfilled over `node:net` (faithful to the Direct Sockets contract) and `chrome.runtime` mocked | live (`vv/suites/cc41-extension-egress`) — the service worker, driven against a real echo server, opens a socket and reports `OPENED`, round-trips `DATA`, and reports `FAILED` for an unreachable host — wire-compatible with `CC-16`/`CC-39` (`crates/holospaces-web/extension/egress-test.mjs`); and the extension builds into a *publishable upload zip* (`scripts/build-extension.sh` — exactly the runtime files: manifest + worker + icons, MV3 + minimal-permission validated, no dev/test/page files), which the Pages deploy also emits at `/extension/` for download — and, until the extension is in the Chrome Web Store, the Manager console links to it with manual-install steps (`download → unzip → load unpacked`)
| CC-42 | The browser provisions a repository's *real* OCI image into a bootable rootfs — the deployed Pages path, not a demo | the OCI distribution pull (`CC-20`) run *in the tab* over a router transport, and a real image fixture as the differential — the deployed implementation that makes a launched holospace the repository's actual devcontainer | a browser tab cannot run a blocking HTTP client and the registries are CORS-blocked, so the pull is the *same* transport-agnostic [`ImagePull`](crate::oci) the native importer (`CC-20`) uses — one OCI client, two transports — driven by a fetch/deliver loop: the page reads `nextUrl`/`nextAccept`/`nextBearer`, fetches through the *router extension's* CORS-free fetch (`CC-41`), and feeds the bytes back; `ingest` re-derives every blob (Law L5) and `assemble` yields the ext4 rootfs the emulator boots over `virtio-blk`, routed for egress (`ChannelEgress`). The page-driven pull yields the *identical* verified image as a direct ingest (proven natively); and the whole path runs in a real browser, with the same-origin test server standing in for the router (the pull logic is identical). *Deployed:* the Manager provisions on Enter through the router, stages the rootfs in OPFS (`provisioned/<id>`), and `holospace-fs` boots it (`boot_devcontainer_routed`); the language-server base image is now strictly the workbench-machinery test fixture / un-provisioned default — a real launch without the router is gated with an honest prompt, never shown the fixture as its repo | live (`crates/holospaces/src/oci.rs` `the_page_driven_pull_matches_a_direct_ingest`; `crates/holospaces-web/web/manager-test.mjs` drives `DevcontainerProvision` in Chromium against a real image fixture → a bootable ext4 rootfs)
| CC-43 | The system emulator supports three guest architectures (riscv64, aarch64, x86-64) | the three ISAs holospaces provisions + boots — RISC-V `RV64GC` (`CC-9`), ARM `AArch64` (`CC-36`), and **x86-64/AMD64** (this) — each a content-addressed *identity* (`Arch`, ADR-021) and a CPU core over the *shared* device substrate (κ-disk/`virtio`/console, Law L4) | the architecture is fixed at provisioning and part of the holospace's κ (the same repo under three ISAs is three distinct holospaces); the OCI pull selects `linux/{riscv64,arm64,amd64}` accordingly, and all three assemble onto the paged κ-disk. The x86-64 core (`crate::emulator::x64`) is a long-mode integer CPU over the shared bus — REX/ModRM/SIB-decoded arithmetic, control flow, the stack, and the 16550 serial console — built up instruction-by-instruction (the analogue of the RISC-V riscv-tests); the RISC-V and AArch64 cores boot real Linux to userspace today, and the x86-64 boot path composes on this core | live (`vv/suites/cc43-x64-core` — real x86-64 machine code decoded + executed: arithmetic, a branch loop setting the zero flag, serial output, push/pop; `crates/holospaces/src/emulator/x64.rs`)
| CC-44 | A real amd64 (x86-64) Linux kernel boots to userspace on the emulator | a stock upstream x86-64 Linux kernel + a minimal userland, with `qemu-system-x86_64` as the differential oracle (the `CC-36` / `CC-9` pattern) — OPM SD2 *Booting*, the x86-64 realization | the conceptual model is arch-agnostic (the ISA is a property of the Holospace, ADR-021), so the third ISA must boot real Linux to userspace like RISC-V (`CC-9`) and AArch64 (`CC-36`). Built on the `CC-43` x86-64 core: the 64-bit Linux boot protocol, `virtio-mmio` κ-disk servicing over the *shared* `emulator::devbus`, and interrupt delivery (IDT) — byte-faithful to `qemu-system-x86_64` | live (`vv/suites/cc44-x64-linux` — the x86-64 core boots a real, unmodified amd64 Linux 6.6 to userspace: `Run /init`, `HOLOSPACES-LINUX-USERSPACE-OK`, and the real `/proc/version`, byte-identical to `qemu-system-x86_64`; witness `crates/holospaces/tests/cc44_x64_boot.rs` `an_amd64_linux_kernel_boots_to_userspace`, `#[ignore]`d release, run explicitly by the suite)
| CC-45 | An amd64 devcontainer runs the ecosystem's stock `linux-amd64` binaries | the Dev Container + OCI specs over an `amd64/linux` image, a stock unmodified `linux-amd64` busybox, and `qemu-system-x86_64` as the differential — the x86-64 analogue of `CC-37` | an `amd64` holospace boots (`CC-44`) from a κ-addressed `amd64` image over the shared `virtio` bus and runs *unmodified* `linux-amd64` binaries with no per-ISA workaround (Law L4) — busybox as PID 1 (`uname -m` → `x86_64`, a shell compute, `head` on `/proc/version`) | live (`vv/suites/cc45-x64-devcontainer` — a stock, unmodified `linux-amd64` busybox boots on the x86-64 core from the κ-disk `virtio-blk` rootfs and runs as PID 1 with no per-ISA workaround (Law L4): `CC45-DEVCONTAINER-UP`, `uname -m` → `x86_64`, a shell compute → `CC45-COMPUTE:500500`, `head` on the real `/proc/version`, then a clean `poweroff` (`Halt::Halted`) — byte-matching the `qemu-system-x86_64` differential oracle; the build-capable κ-disk indexes O(content), so an ≥ 8 GiB disk boots promptly. The full Dev Container / OCI contract feeds the x86-64 boot, each witnessed by the suite: arbitrary *multi-layer* images (OCI whiteout + override + add), a *Dockerfile build* (`CC-26`: `FROM`/`ENV`/`COPY`/`RUN` in the booted OS), and *features* (`CC-25`) + *lifecycle* hooks (`CC-22`) in spec order — plus the streamed-from-`KappaStore` paging the browser peer's `X64Workspace` uses (compact, O(content) staging — an 8 GiB disk stages as a few-MiB OPFS file). The devcontainer is *build-capable*: a static `linux-amd64` toolchain (TinyCC) *compiles a program inside the guest and the guest runs the binary it built* — the decisive "you can actually develop in it" bar, not merely bootable. Witnesses in `cc44_x64_boot.rs` (`an_amd64_devcontainer_runs_a_stock_linux_amd64_binary`, `…_multilayer_image_overlay_runs`, `…_dockerfile_build_runs_on_x64`, `…_features_and_lifecycle_run_on_x64`, `…_boots_paged_from_a_kappa_store`, `…_boots_occupancy_streamed_from_a_large_disk`, `the_deployed_amd64_init_boots_occupancy_streamed_from_a_large_disk`, `an_amd64_devcontainer_builds_a_program_in_guest`, `an_arbitrary_real_amd64_oci_image_boots_on_x64`, `the_amd64_devcontainer_shares_the_9p_workspace_with_the_editor` — the shared `virtio-9p` workspace mounted by a real amd64 guest, files round-tripping in both directions with the editor — `holospaces_parses_its_own_unmodified_devcontainer_config`), `#[ignore]`d release, run explicitly by the suite; the deployed amd64 provision→boot is witnessed in Chromium by `cc45-x64-boot-test.mjs`. A *blank* holospace boots the deploy's *bundled* per-arch busybox layer *in-RAM* — no registry pull, no router extension — so the default amd64 scratchpad launches to a shell + editor + tasks + search immediately (`X64Workspace.boot_devcontainer` / `Aarch64Workspace.boot_devcontainer`); a repository's real registry image still provisions via the router. *Dogfood:* (on-demand heavy suite, not in the per-push gate — needs docker + ~24 GiB RAM) `vv/heavy/cc45-dogfood-devcontainer` builds this repo's *own* unmodified `.devcontainer` (Ubuntu 24.04 + toolchain, Dev Container CLI) and `cc45_dogfood::holospaces_builds_in_its_own_real_devcontainer` boots it on the x86-64 core where the real gcc 13.3 (cc1 → as → ld, glibc 2.39) compiles a program in-guest and the guest runs it (`DOGFOOD-GCC-BUILT:42`). *KNOWN DEFECT (fork/exec-specific, under investigation):* under sustained *fork/exec* load the x86-64 guest's heap corrupts after ~1500 s of real execution (`malloc(): unsorted double linked list corrupted`) — the same instruction-level emulator-defect family the dogfood surfaced (CPUID/CMPXCHG/SSE). It is *fork/exec-specific*: a pure busy-spin runs 5x past the crash point clean, a pure idle guest runs to 100000 s clean, and it survives a full software-TLB bypass, so it is a rare per-instruction mis-execution on the `fork`/`exec` path (needs a qemu differential trace to pinpoint). Two contributing correctness bugs found while investigating are fixed: the TSC is now *invariant* (it had taken a per-cache-miss timing charge that drifted it against the PIT-calibrated clock) and a faulting write no longer scribbles guest-physical frame 0; the boot stays byte-identical to the qemu oracle)
| CC-46 | The shared device bus serves the 9p workspace, the network, and the guest bridge on the AArch64 core at real-OS caliber, with x86-64 at device-level (arch device parity) | the `virtio-9p` / `virtio-net` specs and the `CC-15` / `CC-16` / `CC-33` authorities, run on the AArch64 core at real-OS caliber (a real arm64 Linux boot) with x86-64 covered at device-level (the MMIO transport over the shared devbus) — OPM SD3 *Sync* + SD4 *Working*; Law L4 (devices are *shared*, not per-ISA) | today only the RISC-V machine wires `virtio-9p` (`CC-15`), `virtio-net` + NAT (`CC-16`), and the guest bridge (`CC-33`); `emulator/aarch64.rs`'s `Sys` wires only `virtio-blk`, and the x86-64 core none. This moves 9p/net/bridge into the shared `emulator::devbus` and wires the AArch64 + x86-64 cores — the V&V that makes `CC-37`/ADR-021's "shared 9p+net+bridge" claim *true on the AArch64 core at real-OS caliber, with x86-64 at device-level (the MMIO transport over the shared devbus)* | live (`vv/suites/cc46-devbus-arch-parity`) — the **extraction is done**: `virtio-9p`, `virtio-net` + the userspace NAT, and the in-process guest bridge are extracted into the shared `emulator::devbus` (core-agnostic, over `GuestRam`), and *both* the *AArch64* core (the `virtio-9p` + `virtio-net` MMIO slots raising the GIC, INTID 49/50) *and* the *x86-64* core (the same slots over its own MMIO window at `0xD000_0000`) wire that one devbus and expose the bridge — one devbus, *three* MMIO transports (RISC-V PLIC, AArch64 GIC, x86-64), *no per-ISA device re-implementation* (Law L4); the RISC-V `CC-15`/`CC-16`/`CC-33` suites stay green (behavior-preserving). Witnessed at **real-OS caliber** (the same caliber as the `CC-15`/`CC-16`/`CC-33` rows) by `cc46_realboot.rs`: a **real arm64 Linux** boots over the shared devbus and a real guest userspace *mounts the holospaces `virtio-9p` workspace through its VFS* and round-trips a file (`CC-15` parity), *opens an outbound TCP/HTTP flow over `virtio-net` through the userspace NAT* to a real host server (`CC-16` parity), and *serves a real listener* (busybox `httpd`) reached from the host over the *in-process bridge* — `dial_guest` → `guest_send`/`guest_recv`, no socket, no relay (`CC-33` parity). The device-level regression check `crates/holospaces/tests/cc46_devbus_parity.rs` drives each core's own `virtio-mmio` device path through the shared devbus (fast coverage in the default test set); and the x86-64 core is now ALSO witnessed at *real-OS caliber* for the 9p workspace: `cc44_x64_boot.rs::the_amd64_devcontainer_shares_the_9p_workspace_with_the_editor` (the `CC-45` suite) boots a real amd64 Linux that *mounts the share and round-trips files in both directions* (host-written read in-guest; guest-written observed by the editor; the nested-path tree the workbench drives)
| CC-47 | The lifecycle (suspend → κ snapshot → resume) works on *every* core (arch parity) | the `CC-30`/`CC-31` snapshot model applied to the AArch64 + x86-64 cores — OPM SD2 *Lifecycle* ("Suspending yields Snapshot; Resuming requires Snapshot → Holospace") | suspend/resume from a κ snapshot is witnessed on RISC-V only; this brings the AArch64 + x86-64 cores to parity — a running guest suspends to a content-addressed snapshot (CPU + RAM + κ-disk) and resumes byte-identically, execution continuing | *target — not yet live* (`vv/targets/cc47-lifecycle-arch-parity`; witness `crates/holospaces/tests/cc47_lifecycle_parity.rs` — boot · run · suspend · drop · resume · continue)
| CC-48 | The substrate-native extension host activates an *arbitrary* marketplace extension | the VS Code remote-server protocol + extension API; an arbitrary workspace/Node extension from Open VSX as the unmodified subject — OPM SD4 *Working*; closes the frontier named by `CC-19`/`CC-34`/ADR-020 | holospaces is the VS Code remote (in the tab, on the substrate); the server-backed editor capabilities (LSP over the `CC-33` bridge, `CC-18`) are live, and the *extension host* that activates arbitrary marketplace extensions is *now live too*. holospaces provides it as *native hologram exec on the browser peer's wasm execution surface* (a JS/Node-API runtime on the browser peer's own JS engine; replacing the legacy Node `vscode-server`, Law L4), backed by the holospace's own filesystem (`CC-15`), terminal (`CC-11`), network (`CC-16`) — *not* `openvscode-server` inside the emulated guest (the "interpreter wall") and *not* vscode-web's web ext host (that is `CC-19`); the extension + its bundled deps come content-addressed from Open VSX (the `.vsix`) | live (`vv/suites/cc48-ext-host`) — a *Node-only* Open VSX extension (`editorconfig.editorconfig`, committed + sha256-pinned in `vv/artifacts/cc48`, `main` and *no* `browser` entrypoint so it cannot run in the web host) downloads from Open VSX, unzips, and *runs `activate()` in the substrate-native (wasm-exec) Node-API extension host* — its `main` runs on the browser peer's own JS engine, the Node API surface backed by the holospace's own filesystem (`CC-15`)/terminal (`CC-11`)/network (`CC-16`) and the `vscode` API passed through to the real workbench; the host publishes `HOLOSPACE-NODE-EXTHOST-LIVE` (a status-bar item, in the DOM) *only after a genuine `activate()` returns* — witnessed in Chromium (`crates/holospaces-web/web/ext-host-test.mjs`), no emulated-guest server, no Node on the host, no deployment outside the holospace (Law L4)
| CC-49 | Two browser peers exchange κ-content over a *real* WebRTC data channel | the substrate's content-addressed network (`BareNetSync`, verify-on-receipt) over the portable `NetworkInterface`, carried by a real `RTCDataChannel` — OPM SD3 *Sync*; closes the transport frontier named by `CC-38`/ADR-006 (no central operator) | `CC-38` proved the protocol is identical across native/wasm/bare-metal with an in-test pump for the link; this carries the **full frame set — `announce` / `discover` / `fetch`** — between two browser tabs over a real WebRTC channel: one peer announces a κ it holds, the other discovers that holder and fetches the κ, verified by re-derivation before acceptance (a forging responder rejected; an unheld κ absent) | live (`vv/suites/cc49-webrtc-content-net`) — the witness `crates/holospaces-web/web/webrtc-content-net-test.mjs` connects two browser contexts over a **real `RTCDataChannel`** (out-of-band SDP/ICE signaling, no server) and drives **all three** content-network frames over it: peer B **announces** a κ it holds (`cn_announce` + `cn_pump`), peer A **discovers** that holder over the channel (`cn_discover` + `cn_pump`), then A **fetches** the κ and accepts it only after re-derivation; a forging responder's bytes (`holospaces-web::Console::new_forging` over `content_net::forging_peer`) are rejected, an unheld κ resolves to nothing, and the exchange is symmetric. The transport is wired into the **product**, not the witness: `holospaces-web::WebRtcLink` is the real `RtcDataChannel`, and the product pump `holospaces-web::Console::cn_pump` couples it to the `BareNetSync`-driven `content_net::PacketLink` `NetworkInterface` (the *same* one a bare-metal peer drives, `CC-38`) — so the witness exercises the product path (`cn_announce` / `cn_discover` / `cn_fetch_start` + `cn_pump` + `cn_fetch_poll`), and the deployed Manager UI (`crates/holospaces-web/web/index.html`, the *Peer content network* panel) lets a real operator announce, discover and fetch a κ from another browser peer over WebRTC. The forging-responder rejection is also pinned in the gated native tier (`crates/holospaces/tests/cc38_content_net.rs`)
| CC-50 | Provisioning assembles an arbitrarily large rootfs without materializing a dense in-memory image | the ext4 on-disk format (a valid bootable filesystem) and the substrate's content-addressed store as the medium; differential = the streamed assembly is *κ-identical* to the dense assembly — OPM SD5 *Rootfs Assembly* ("the KappaStore IS the memory, RAM is a cache") | a holospace's disk is content-addressed sectors paged from a `KappaStore` (`CC-42`), and provisioning now *streams* assembly sector-by-sector into the `KappaStore` — sparse, deduped, never a dense image. The single canonical ext4 serializer (`assembly/ext4.rs`) emits only the *non-zero 4 KiB blocks* (holes and the free data region stay sparse); the dense (`write_image_with_free`) and streaming (`stream_image_with_free` / `sparse_blocks_with_free`) consumers are byte-identical by construction. `stream_ext4_bootable_into_disk` (and the raw-byte `stream_ext4_image_bootable`, wired into the browser as `DevcontainerProvision.assembleIntoOpfs`) build a `KappaDisk` over `KappaDisk::from_block_stream`; a written all-zero sector is treated as sparse, so the dense `from_image` path and the streamed `from_block_stream` path yield the *same sector κ-set* for the same content. So an image larger than the tab's heap provisions and boots, peak memory bounded by content and κ-identical to the dense path | live (`vv/suites/cc50-streaming-rootfs-assembly`; host witness `crates/holospaces/tests/cc50_streaming_assembly.rs` — a 512 MiB sparse rootfs assembles materializing only ~0.8% of the image, the streamed κ-disk is κ-identical to and byte-identical with the dense path, and `e2fsck` finds the streamed image clean; *deployed-path witness* `crates/holospaces-web/web/cc50-streaming-boot-test.mjs` (browser job) — in Chromium a dedicated worker streams the bootable rootfs straight into an OPFS file via the shared `stream_ext4_image_bootable` serializer the wasm `assembleIntoOpfs` uses (a 512 MiB sparse disk) and *BOOTS it to a userspace marker* through the shipped `Workspace.boot_devcontainer_routed_opfs_streamed` paged-κ-disk path, so the streamed-into-OPFS image is proven to boot for real — not only κ-identical)
| CC-51 | The workbench's Source Control view reflects, commits, and pushes the *real* repository over the holospace's own primitives (Git/SCM parity) | the VS Code https://code.visualstudio.com/api/extension-guides/scm-provider[SourceControl (`scm`) API] + the https://git-scm.com/docs/gitformat-pack[Git on-disk object] and https://git-scm.com/docs/pack-protocol[pack-protocol] formats as the authority a commit's bytes re-derive against (Law L5), with https://github.com/isomorphic-git/isomorphic-git[isomorphic-git] — a faithful, spec-conformant Git implementation — as the engine; the Codespaces/Gitpod SCM UX — OPM SD4 *Working* | `holospace-fs` registers *no* `SourceControl` provider (the `scm` count is 0): the editor shares the workspace over 9p (`CC-15`), git runs in the guest (`CC-16`/`CC-20`), and holospaces is the remote over the bridge (`CC-33`/`CC-34`), but the Source Control view is empty. This adds a builtin SCM provider (`holospace-scm`) that runs a real Git engine as *native exec on the browser peer* (the `CC-48` discipline — heavy in-tab work is native peer exec, never the emulated guest) over the holospace's *own* `virtio-9p` workspace (`CC-15`) — the *same* `.git` content the guest's `git` reads (one content, Law L1). So the working-tree *status* (modified / added / deleted / untracked), the *quick-diff* gutter + the diff editor *vs HEAD*, *stage / unstage*, *commit* (a real Git commit object), the *branch* indicator + *switch / create*, and *push / pull* (the smart-http pack-protocol over the browser peer's own network, with the operator's auth — `CC-24`) all work — and a commit made in the UI is a real commit in the repository's `.git` the guest shares, never a server outside the holospace (Law L4). The host-side 9p workspace API is extended to *nested paths* (the `.git` object tree), at `CC-15` parity with the guest's `Twalk`/`Tcreate` | live (`vv/suites/cc51-scm-git`) — the builtin `holospace-scm` provider registers a real `vscode.scm` SourceControl whose engine is *isomorphic-git* (a faithful, spec-conformant Git implementation; κ-pinned, re-derived to its sha256 before it runs) running as native browser-peer exec over the holospace's own `virtio-9p` workspace (`CC-15`). *Host witness* `crates/holospaces/tests/cc51_nested_workspace.rs` (a real busybox boot): the host writes a nested `.git/objects/ab/cdef` tree the guest reads back byte-identically over `virtio-9p`, and the guest writes a nested file the host reads back — one nested content, both directions (Law L1). *Deployed-path witness* `crates/holospaces-web/web/scm-git-test.mjs` (Chromium): the SourceControl provider is *live* in the real workbench (the `scm` count 0 → a registered provider), reflects the *real* repository (the workspace's files appear as working-tree changes over 9p), a *commit* through the SCM input *re-derives* to the canonical Git object (sha1 of `commit <len>\0<content>` == the engine's oid) with HEAD pointing at it (Law L5, the object-format authority), and a *push* over the smart-http pack-protocol *lands in a real `git http-backend` bare repo* — the independent oracle `git log` shows the same sha + message
| CC-52 | The workbench's Search view finds and replaces across the *real* workspace (find-in-files parity) | the VS Code https://code.visualstudio.com/api/references/vscode-api#FileSearchProvider[`FileSearchProvider`] + https://code.visualstudio.com/api/references/vscode-api#TextSearchProvider[`TextSearchProvider`] search-provider API, with the workspace's *actual file content* as the authority a match re-derives against (a reported match must occur at its line/column in the file the guest shares — Law L5); the Codespaces/Gitpod find-in-files UX — OPM SD4 *Working* | `holospace-fs` registers *no* search provider (`findFiles` / `registerTextSearchProvider` / `registerFileSearchProvider`: 0), and the web workbench has *no* fallback search for a virtual scheme (it dispatches per-scheme; a `holospace://` workspace with no provider returns nothing), so find-in-files is dead. This adds a builtin `holospace-search` that registers a *file* and a *text* search provider — *native exec on the browser peer* (the `CC-48` discipline, never the emulated guest) over the holospace's *own* `virtio-9p` workspace (`CC-15`): a query *streams* matching files + lines with context (`Progress`-reported, non-blocking on a large tree); *include/exclude globs* and *`.gitignore`* are honored (`useIgnoreFiles`); and *search & replace across files* applies, the edits landing in the 9p workspace — the *same* content the guest sees (one content, Law L1). Never a server outside the holospace (Law L4) | live (`vv/suites/cc52-search`) — the builtin `holospace-search` registers a File + Text search provider running as native browser-peer exec over the 9p workspace (`CC-15`). *Core witness* `builtin-extensions/holospace-search/search-core.test.cjs` (Node, deterministic, 30 checks): VS Code glob include/exclude (`+**+`, `+*+`, `?`, `{a,b}`, `[...]`), `.gitignore` via the vendored `ignore` (negation, dir-only, anchoring), the *streaming + cancelling + excludes-pruning* tree walk with `maxResults`/`limitHit`, and literal/regex/case/word/multiline matching whose reported `(line,col)` *slices to exactly the matched text* (Law L5). *Deployed-path witness* `crates/holospaces-web/web/search-test.mjs` (Chromium, the real workbench): the providers are *live* (the workbench has *no* fallback search for a virtual scheme, so this is what makes find-in-files work), a query for `greet` returns the *expected* matches (the workbench's own "N results" count) and the Search view *renders the result rows*, and a *replace-all* *edits the file over 9p* — with the editors closed (so the provider re-reads the *settled* file), a re-search confirms every match is replaced
| CC-53 | The workbench runs `tasks.json` tasks in the devcontainer — output, exit status, problem matchers, background tasks (Tasks parity) | the VS Code https://code.visualstudio.com/api/extension-guides/task-provider[Task provider API] + the https://code.visualstudio.com/docs/editor/tasks[`tasks.json`] schema, with the task command's *real execution in the guest devcontainer* as the authority — a task that exits `N` reports `N`, its stdout/stderr are the bytes the guest wrote, and a problem-matcher line becomes a diagnostic at its file/line — OPM SD4 *Working* | `holospace-fs` registers *no* task provider (`registerTaskProvider`: 0), and the web workbench *disables* shell/process task execution in a virtual workspace (the `virtualWorkspace` context gates `shellExecutionSupported`/`processExecutionSupported` off), so `tasks.json` is dead. This adds a builtin `holospace-tasks` that parses `.vscode/tasks.json` (over 9p, `CC-15`) and registers a `TaskProvider` whose tasks run as `CustomExecution`: a `Pseudoterminal` runs each task's command *in the guest devcontainer* (`CC-11`) over a file-exec channel on the 9p workspace — a tiny guest task-runner agent (a `/bin/sh` loop seeded into the devcontainer `/init`) runs the command and streams its output + exit code back over 9p (`CC-15`/`CC-11`; image-agnostic — only `sh` + the share), never a server outside the holospace (Law L4). Output + exit status surface in the task terminal; the task's *problem matchers* populate the Problems panel (VS Code feeds a `CustomExecution`'s pty output to the same problem collector as a shell task); *background/watch* tasks run non-blocking with the spinner | live (`vv/suites/cc53-tasks`) — the builtin `holospace-tasks` registers a `TaskProvider` whose tasks run as `CustomExecution` over the guest file-exec channel (the task-runner agent in the devcontainer `/init`). *Core witness* `builtin-extensions/holospace-tasks/tasks-core.test.cjs` (Node, deterministic, 27 checks): the `tasks.json` (JSONC, string-aware) parse, the devcontainer.json *lifecycle-command* surfacing (string / argv / named-object forms; host-side `initializeCommand` excluded), the shell-command build (cwd / env / arg-quoting), and the file-exec request/stream/exit protocol. *Deployed-path witness* `crates/holospaces-web/web/tasks-test.mjs` (Chromium, the real workbench): the provider is *live*, the default build task *runs in the devcontainer* — the guest task-runner agent executes it and its output + *non-zero exit status* (`code 2`, read from the agent's `<id>.exit` over 9p) are captured — the task's *problem matcher* turns its `main.rs:2:5: warning: …` line into a *diagnostic* in the Problems panel, the devcontainer's *lifecycle commands* (`CC-22`) surface as re-runnable tasks (the seeded `postCreateCommand` runs in the guest, exit 0), and a *background/watch* task runs *without blocking* — the guest agent executes tasks concurrently, so the build task runs to its exit while watch keeps streaming
|===

The capability set above is the **Codespaces/Gitpod parity** target (ADR-016): a repository becomes a ready, real development environment with the authentic VS Code workbench, extensions and their integrations, language intelligence and debugging, port-forwarded app previews, dev-container Features and lifecycle, and personalization — realized over the substrate with no server. ADR-016 maps each capability to its conformance row and substrate primitive so the model has no gaps against Codespaces/Gitpod.

The `CS` rows are witnessed green by every documentation build. The `CC` rows are each witnessed by a suite in `vv/suites/` that validates the component against its external authority — never against itself — by composing the https://github.com/Hologram-Technologies/hologram[hologram] substrate (consumed by reference, ADR-006), at the rev pinned in the workspace `Cargo.toml`. Adding a component without satisfying its `CC` row leaves holospaces incomplete by definition.

`CC-47` .. `CC-49` are *targets* (behavior/test-driven; `CC-44` and `CC-45` were each promoted to `live` when their boot went green). Each is unfinished work whose *behavioral V&V is written first* — an executable, expected-RED suite in `vv/targets/` (and a named `+#[ignore]+`d witness) that defines "done" before any implementation. Each realizes a named OPM process (`Conceptual-Model`) for an architecture or surface not yet covered, so the build is *to a spec*, never shot in the dark. The target tier is *non-gating* — a RED target never fails V&V or blocks deploy (the green gate is the `live` rows only); when a component is built to its target and the suite goes green it is *promoted* from `vv/targets/` to `vv/suites/` and its row turns `live`. A target whose analysis shows it is not required (e.g. homomorphic encryption, chapter 13 §13.9) is removed, not built — targets are derived from the model and the threat analysis, not assumed.

=== Quality gates and test tiers

The quality commitments above are enforced from the first commit by continuous integration (`.github/workflows/ci.yml`), which runs on every change:

* *V&V* — `vv/run.sh`: CS-1..CS-6 specification conformance (V1–V8) and the CC-1..CC-11 component suites (`vv/suites/`), each against its external authority.
* *Format* — `cargo fmt --check`.
* *Lints* — `cargo clippy` with warnings denied; the workspace forbids `unsafe_code` and warns on `missing_docs`.
* *Docs* — `cargo doc` with warnings denied (no broken links; every public item documented).
* *Unit* — `cargo test --lib`: a building block in isolation.
* *Integration* — `cargo test --test integration`: building blocks composed.
* *End-to-end* — `cargo test --test e2e`: whole operator flows (over the real substrate runtime and a real content-addressed gateway).
* *Portability* — the peer core builds for every supported environment: native, browser (`--target wasm32-unknown-unknown`), and bare-metal (`--no-default-features --target thumbv7em-none-eabi`, `no_std`). The same holospace κ boots on any peer (Chapter 7; quality goal Q6).
* *Browser* — `scripts/browser-manager-test.sh`: the Hologram Platform Manager (the browser peer) runs in headless Chromium (Playwright) — sign in, provision (both compute forms), view, resolve + verify by re-derivation (Law L5), a live `.holo`-engine differential (the executor compiled to wasm yields a κ identical to native — `CC-2`, RT2), and the execution surface — a κ-addressed Wasm code module is validated against the host-ABI surface, provisioned, **and booted as a container in-browser** through hologram's `wasmi` interpreter engine (suspended to a κ snapshot, resumed, terminated — `CC-6`, ADR-008's contract generalized by ADR-009; an ambient import is refused). It is deployed to GitHub Pages by `.github/workflows/pages.yml` (the cold-start, untrusted gateway of Chapter 6).
* *Execution surface* — `cargo test --test cc6_execution_surface`: a κ-addressed Wasm code module (the surface the system emulator binds; ADR-008's contract, generalized by ADR-009) is validated against the WebAssembly specification and the substrate host-ABI surface, then booted, suspended, resumed, and migrated on the real `ContainerRuntime` over **both** engines — the native Wasmtime JIT and hologram's `wasmi` interpreter (the browser + bare-metal engine) — yielding the same holospace κ on each (Q6). A devcontainer holospace boots the code its config selects (`cargo test --test e2e`). The RT1 surface (`CC-6`).

Every `CC` suite and test tier witnesses its component against its external authority on every change; each runs from the first commit.
