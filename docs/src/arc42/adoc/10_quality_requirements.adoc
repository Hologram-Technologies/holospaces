[[section-quality-scenarios]]
== Quality Requirements

=== Quality Requirements Overview

The quality goals (Chapter 1) derive from the laws (Chapter 2):

[options="header"]
|===
| Attribute | Requirement
| *Integrity* | Every accepted byte is re-derived against its κ (Law L5).
| *Reproducibility* | A holospace's identity is the κ of its definition; identical inputs yield identical holospaces.
| *Portability* | The same holospace κ boots on any peer (browser / native / bare-metal).
| *Efficiency (memory)* | The store is the address space; content dedupes; RAM is a bounded cache (Law L3).
| *Autonomy (no server)* | No host, no account, no control plane; peers are content-addressed (Law L1).
| *Authority* | The documentation is the single authoritative source; features trace to it.
|===

=== Quality Scenarios

[options="header"]
|===
| # | Scenario | Expected response
| QS1 | The same git repo + `devcontainer.json` is provisioned twice, on different peers. | Both yield the same holospace κ.
| QS2 | A holospace is suspended on one signed-in instance and resumed on another. | It resumes from the κ snapshot; unchanged state dedupes.
| QS3 | A gateway (e.g. GitHub Pages, or a peer) returns bytes that do not match the requested κ. | The bytes are rejected on re-derivation; the fetch fails over to another source.
| QS4 | A holospace's total state exceeds available RAM. | It still boots; content is demand-paged by κ-resolve, evicted by garbage collection.
| QS5 | An operator signs in on a new instance. | Their holospaces and state are discoverable and synchronise over the substrate.
| QS6 | A contributor implements a feature. | The feature links to the documentation section it realizes; the V1–V8 pipeline passes.
| QS7 | An unmodified Linux devcontainer is provisioned and booted. | Its real operating system boots to userspace and runs its real binaries (a shell, `apt`); behaviour matches the Linux specification.
| QS8 | An operator edits a file and runs a terminal command in a holospace's workspace projection. | The edit and command publish as canonical events on the holospace's channels; the κ snapshot advances; the editor and file tree reflect the new content (read by κ).
| QS9 | An operator signs in to the Platform Manager and organizes their holospaces, performing lifecycle operations (provision · enter · suspend · resume · terminate). | The console authenticates a self-sovereign identity, lists and organizes the holospaces with status, and each operation runs through the real browser peer; the console holds no state of its own (the holospaces and roster are κ in the store).
| QS10 | An operator *enters* a holospace from the Platform Manager. | A new browser tab launches the VS Code experience — the real Monaco editor and xterm.js terminal, integrity-verified — over the running devcontainer: the terminal drives the booted OS, the editor and file tree read/edit content by κ.
| QS11 | The devcontainer boots its operating system. | A real distribution root filesystem mounts over a VirtIO block device from the κ-disk; the terminal runs the real binaries (a shell, the coreutils) — behaviour matching the Linux specification and QEMU.
| QS12 | The operator edits a file in the editor and reads it in the terminal. | The editor and the OS share the workspace filesystem over VirtIO-9p (9P2000.L); the edit (content by κ) is the same file the terminal reads — one content, no copy.
| QS13 | The devcontainer runs its `devcontainer.json` `features` / `postCreateCommand`. | The OS reaches the network over VirtIO-net and the package manager installs the real packages; the setup commands run, matching the Dev Container specification.
| QS14 | The operator uses the workspace's language intelligence and extensions. | The real VS Code workbench (κ-verified) speaks the Language Server and Debug Adapter protocols to tools running in the devcontainer OS, and runs extensions the `devcontainer.json` selects — the authentic Codespaces/Gitpod experience.
|===

=== Verification and Validation

holospaces is evaluated against *external authoritative specifications and standards* — never against itself (no self-reference). The V&V is *defined by this documentation* and is authoritative: each invariant below names the external authority it is validated against, the mechanism that enforces it, and the witness. Real validation artifacts are imported and pinned, with provenance recorded in `vv/PROVENANCE.md`; imported artifacts are themselves content-addressed and verified on import. The executable framework lives in `vv/` and is the single V&V entry point (`vv/run.sh`).

The framework has two tiers:

* *Specification conformance* (`CS`) — the holospaces specification (this documentation) validated against the standards it is authored under. Live on every build.
* *Component conformance* (`CC`) — each component validated against the external authority for its behavior. Each is specified here and witnessed by its suite against that authority, never by self-reference.

=== Conformance catalog

[options="header"]
|===
| ID | Invariant | External authority | Enforcement | Witness
| CS-1 | Architecture structure conforms to arc42 | https://arc42.org[arc42] template (pinned) | V1, V2 | every doc build (live)
| CS-2 | The C4 model is well-formed | Structurizr DSL | V3 | every doc build (live)
| CS-3 | Rendered docs are valid GitHub-flavoured Markdown | CommonMark / GFM; GitHub-markup | V4, V5 | every doc build (live)
| CS-4 | The conceptual model is valid OPM | OPM (ISO 19450) OPL grammar | V6 | every doc build (live)
| CS-5 | Each OPD agrees with its OPL | OPM (ISO 19450) bimodality | V7 | every doc build (live)
| CS-6 | The lifecycle covers the standard processes | ISO/IEC/IEEE 15288 | V8 | every doc build (live)
| CC-1 | κ-labels are correct content addresses | the σ-axis hash standards (BLAKE3; FIPS 180-4 SHA-2; FIPS 202 SHA-3; Keccak) via their reference implementations | byte-for-byte equality with the reference σ-axis; re-derivation (Law L5) | live (`vv/suites/cc1-kappa-addressing`)
| CC-2 | The browser `.holo` engine equals the native one | the native https://github.com/Hologram-Technologies/hologram[hologram] executor as oracle (identical `.holo` yields identical κ) | differential check | live (`vv/suites/cc2-holo-engine`)
| CC-3 | A peer's storage obeys the substrate contract | the https://github.com/Hologram-Technologies/hologram[hologram] substrate conformance battery (TCK) | run the imported battery | live (`vv/suites/cc3-substrate-tck`)
| CC-4 | A devcontainer holospace matches its source, including its declared customizations | the https://containers.dev[Dev Container] specification (validated against the published `devContainer.base.schema.json`) and the OCI image spec | a reproducible-κ check (Q4); and the config's *managed parameters* are parsed and honoured (schema-validated; malformed values refused): `customizations.vscode.extensions` (the **base extensions** holospaces installs into the workbench from the open gallery, as a Codespace does — `CC-19`; none bundled by default), `forwardPorts` (the ports to forward — `CC-21`), the lifecycle commands `onCreateCommand`/`postCreateCommand`/`postStartCommand`/… (run in the devcontainer OS so it is ready on entry — `CC-22`), and `remoteEnv` (the environment — `CC-23`) — the full Dev Container managed-parameter set | live (`vv/suites/cc4-devcontainer`)
| CC-5 | Wasm code modules are specification-valid | the https://webassembly.org[WebAssembly] specification | module validation | live (`vv/suites/cc5-wasm`)
| CC-6 | A κ-addressed Wasm code module runs on the execution surface, on every peer's engine | the https://webassembly.org[WebAssembly] specification (validity of the code module + its host-ABI surface) and the https://github.com/Hologram-Technologies/hologram[hologram] `ContainerRuntime` contract (it boots, suspends, resumes, and migrates) on both the native Wasmtime engine and the `wasmi` interpreter engine (the browser + bare-metal `ContainerEngine`) | surface validation + lifecycle on the real runtime, native and interpreter — the κ-addressed code-module surface the system emulator binds (ADR-008's contract, generalized by ADR-009) | live (`vv/suites/cc6-execution-surface`)
| CC-7 | The κ-disk preserves a real filesystem | a real on-disk filesystem image (the https://www.kernel.org[Linux] `ext4` / FAT format) | bytes written through the `KappaStore`-backed block device read back byte-identically; the disk image's κ is reproducible | live (`vv/suites/cc7-kdisk`)
| CC-8 | Arbitrary code imported by κ runs over capability-scoped I/O | the https://github.com/Hologram-Technologies/hologram[hologram] driver-import + `ContainerRuntime` contract; a code module's output is the oracle (identical code + input yields identical κ) | import + verify-by-re-derivation, then run real `storage_put`/`get` bounded by the capability set — I/O outside the storage roots / over budget is denied by the real runtime | live (`vv/suites/cc8-import-run`)
| CC-9 | A real operating system boots and runs on the emulator | the https://www.kernel.org[Linux] kernel + the reference RISC-V machine `qemu-system-riscv64` as the differential oracle (the same OS image) | a real, unmodified Linux kernel boots to userspace on the emulator core (the emulator is the SBI firmware); a real binary's output (the userspace marker + `/proc/version`) matches QEMU byte-for-byte; the κ snapshot is reproducible across re-runs and peers | live (`vv/suites/cc9-emulator`)
| CC-10 | A devcontainer's OS image + repository ingest as κ content | the https://containers.dev[Dev Container] and the https://github.com/opencontainers/image-spec[OCI image] specifications | the image's blobs are walked (index → manifest → config + layers) and verified by re-derivation against their OCI `sha256` digests — an OCI content digest *is* a κ-label (Law L5); the validated `devcontainer.json` (`CC-4`) binds to the ingested image into a reproducible source identity (same source ⇒ same holospace κ, Law L1); the ingested image content is held as `CC-7` κ-disk blocks. The booted behaviour of the image is the emulator's (`CC-9`) | live (`vv/suites/cc10-ingestion`)
| CC-11 | The workspace projection renders and drives a running holospace | the editor/terminal surface (file content, terminal I/O): the substrate store + reference hashes for the Editor/FS view, and a real running Linux terminal + `qemu-system-riscv64` as the differential oracle for the Terminal | the editor reads the environment's content by κ (verified by re-derivation) and an edit advances that κ; the operator's terminal commands publish as canonical events that advance the holospace's κ snapshot, and the rendered terminal is byte-identical to QEMU on the same image | live (`vv/suites/cc11-workspace`)
| CC-12 | The Platform Manager is a management console for the holospace lifecycle | a self-sovereign identity (`CC-1` σ-axis hashes) for sign-in, and the validated lifecycle paths the console composes — provision (`CC-4`), enter→boot (`CC-9`), and the workspace projection (`CC-11`) — exercised in a real browser | the operator signs in to a content-addressed identity, the console lists and organizes their holospaces with status, and each lifecycle action (provision · enter · suspend · resume · terminate) is driven through the real browser peer; the console holds no state of its own (Law L3 — the holospaces and roster are κ in the store) | live (`vv/suites/cc12-manager-console`)
| CC-13 | Entering a holospace renders the VS Code devcontainer experience | the **real VS Code editor and terminal components** — the https://microsoft.github.io/monaco-editor/[Monaco] editor and https://xtermjs.org[xterm.js] terminal (the libraries VS Code / Codespaces / Gitpod are built from), pinned and verified by re-derivation (Law L5) — plus the Dev Container spec (`CC-4`) and the running OS terminal (`CC-9`/`CC-11`) | in a real browser, the workspace tab loads the integrity-verified Monaco + xterm.js, boots the holospace's devcontainer OS on the emulator, and presents it as the IDE: the xterm.js terminal drives the running OS (output matches the `CC-11`/QEMU oracle), the Monaco editor + file tree read and edit environment content by κ, and the `devcontainer.json` validates against the spec (`CC-4`) | live (`vv/suites/cc13-vscode-workspace`)
| CC-14 | The devcontainer boots a real OS root filesystem over a VirtIO block device | the OASIS https://docs.oasis-open.org/virtio/virtio/v1.2/virtio-v1.2.html[VirtIO] specification (v1.2 — `virtio-mmio` + `virtio-blk`) and the https://riscv.org/technical/specifications/[RISC-V] PLIC, a real OCI base image (`CC-10`) assembled into an ext4 `CC-7` κ-disk, and `qemu-system-riscv64` as the differential oracle | the emulator's PLIC + `virtio-mmio`/`virtio-blk` device obeys the VirtIO spec (the descriptor/available/used-ring protocol, the modern feature negotiation, the PLIC claim/complete handshake); a real, unmodified Linux kernel mounts the assembled rootfs from `/dev/vda` and runs real userspace, behaviour matching QEMU on the same kernel + rootfs; the boot is byte-for-byte reproducible (Law L1; ADR-011) | live (`vv/suites/cc14-virtio-block`)
| CC-15 | The editor and the running OS share the workspace filesystem | the VirtIO spec (`virtio-9p`) and the https://ericvh.github.io/9p-rfc/rfc9p2000.l[9P2000.L] protocol, with `qemu-system-riscv64` (its 9p server) as the differential oracle | the emulator serves the workspace's files over a spec-conformant `virtio-9p` device (the 9P2000.L message protocol — attach/walk/getattr/lopen/lcreate/read/write/readdir/mkdir); the OS mounts it over `virtio-9p` and reads/writes the same files holospaces holds — a file holospaces places on the share is read by the OS, and a file the OS writes is read back by holospaces (one content, Law L1) (ADR-011) | live (`vv/suites/cc15-virtio-9p`)
| CC-16 | The devcontainer reaches the internet (git clone / apt / npm) | the VirtIO spec (`virtio-net`), the TCP/IP standards (DHCP, ARP, TCP), and `qemu-system-riscv64`'s user-mode network as the differential oracle | the emulator's spec-conformant `virtio-net` device feeds a *userspace TCP/IP NAT* (ARP + DHCP so the guest gets an address; the guest-facing TCP state machine); each guest flow is relayed out over a *pluggable egress transport* — a direct host socket on a native peer, a **WebSocket tunnel to a relay** on the browser peer (no raw NIC in a tab; ADR-014). The guest does DHCP, opens a TCP connection, and completes an HTTP exchange through the NAT, matching QEMU; a real package install runs over it (Dev Container spec, `CC-4`) | live (`vv/suites/cc16-network`)
| CC-17 | Entering a holospace opens the real VS Code workbench over a remote extension host | https://code.visualstudio.com[VS Code] (the real web workbench build + its remote-server/extension-host protocol) and its published https://code.visualstudio.com/api/extension-guides/virtual-documents[`FileSystemProvider`] + terminal APIs, with the Dev Container spec (`CC-4`) | the workspace tab loads the κ-verified (Law L5) VS Code web workbench and presents the authentic UI (activity bar, command palette, settings, source control); the workbench connects over the substrate to a *remote extension host running in the devcontainer OS* (the VS Code remote model, ADR-015), whose `FileSystemProvider` is the `virtio-9p` workspace (`CC-15`) and whose terminal is the holospace console (`CC-11`) (ADR-012/015) | Phase 1 live — the real workbench loads κ-verified (`vv/suites/cc17-vscode-workbench`); Phase 2 live — the editor-side filesystem over the shared `virtio-9p` workspace, content-addressed, a file the editor writes read by the OS over 9p (`vv/suites/cc17-workspace-fs`), *and* the real workbench is served that workspace the **supported way** — Microsoft's own `@vscode/test-web` built-in `FileSystemProvider` (the github.dev/Codespaces mechanism), its `readDirectory` reaching the real editor (`vv/suites/cc17-workbench-fs`, Chromium); the remote extension host (Phase 3) for `CC-18`/`CC-19` planned
| CC-18 | The workbench provides language intelligence | the https://microsoft.github.io/language-server-protocol/[Language Server Protocol] specification; a real language server (running in the devcontainer OS) as the authority | a language server runs in the devcontainer OS (`CC-14`) under the remote extension host (ADR-015) and the workbench speaks LSP to it over the substrate; completions, diagnostics, and go-to-definition for a real source file conform to the LSP spec (ADR-012) | planned (`vv/suites/cc18-lsp`)
| CC-19 | The workbench runs extensions and their integrations, and debugs | the VS Code https://code.visualstudio.com/api[extension API] + its https://code.visualstudio.com/api/references/vscode-api#authentication[authentication provider] contract (the general integration mechanism), the https://microsoft.github.io/debug-adapter-protocol/[Debug Adapter Protocol] specification, and — as one concrete integration witness — the https://docs.github.com/en/rest[GitHub REST API] + https://docs.github.com/en/apps/oauth-apps[OAuth] | *arbitrary* extensions install from the open marketplace (https://open-vsx.org[Open VSX] — the gallery VS Code web uses, since the MS Marketplace ToS forbids non-MS products) and function; holospaces bundles *no* third-party extension by default (no lock-in). An installed extension runs in the extension host and reaches its backing services through the devcontainer's own filesystem (`CC-15`), terminal (`CC-11`), and network (`CC-16`) — *any* extension's published authentication-provider + API works the same way (auth is the user's, over the network — `CC-24`, ADR-017); installing and using the GitHub Pull Requests extension to manage pull requests and issues is one example, not a default; and a debug session drives a real program through a DAP adapter running in the OS, conforming to the DAP spec | foundation live — a real extension *activates in the real workbench's extension host* the supported way (`@vscode/test-web --extensionDevelopmentPath`, its status-bar contribution appearing in the genuine workbench; `vv/suites/cc19-extensions`); the GitHub integration + DAP build on this host
| CC-20 | A devcontainer provisions from a repository URL over the internet | the https://github.com/opencontainers/distribution-spec[OCI distribution specification] (registry pull — manifest + blobs by digest), an HTTP(S) git-host archive (the repository content at a reference), and the https://containers.dev[Dev Container] spec's default (a repository with no devcontainer) — the internet is an *untrusted gateway* (ADR-013) | holospaces fetches a repository's content by URL+reference and pulls the devcontainer's OCI image from its registry, *verifying every byte by re-derivation* (a registry digest *is* a κ; Law L5 — a tampered byte is refused); a repository with no `devcontainer.json` provisions on a default image; the imported content boots as a holospace (`CC-4`→`CC-10`→`CC-7`→`CC-14`). Witnessed *hermetically* (a localhost server serving pinned content — reproducible) and against the *live* internet (a network-gated smoke test) | live (`vv/suites/cc20-import`)
| CC-21 | A server running in the devcontainer is reachable as a forwarded port (the running-app preview) | the OASIS https://docs.oasis-open.org/virtio/virtio/v1.2/virtio-v1.2.html[VirtIO] spec (`virtio-net`) + the TCP/IP standards, with `qemu-system-riscv64`'s user-mode `hostfwd` as the differential oracle; the Codespaces/Gitpod *port-forwarding / preview* capability | the userspace TCP/IP NAT accepts an *inbound* connection on a forwarded port and bridges it to a listener inside the OS (the dual of the `CC-16` egress; the *ingress* seam — a direct host listener on a native peer, a relay-served route on the browser peer, ADR-016). A real HTTP server started in the devcontainer is fetched from outside the OS, byte-identically, matching QEMU `hostfwd` — the dev server / app preview a Codespace surfaces in its Ports view | planned (`vv/suites/cc21-port-forward`)
| CC-22 | The devcontainer's Features and lifecycle commands run on create | the https://containers.dev[Dev Container] specification's https://containers.dev/implementors/features/[Features] + the lifecycle hooks (`onCreateCommand`, `updateContentCommand`, `postCreateCommand`, `postStartCommand`, `postAttachCommand`) | holospaces applies the `devcontainer.json` Features (each an OCI artifact, imported + verified by re-derivation — Law L5) into the assembled rootfs and runs the lifecycle commands *in the booted OS* in spec order (`CC-14` + the OS terminal `CC-11`); a real repository's `postCreateCommand` (e.g. installing dependencies) runs and its effect is observable in the workspace (`CC-15`) — the environment is *ready on entry*, as in a Codespace/Gitpod | planned (`vv/suites/cc22-devcontainer-lifecycle`)
| CC-23 | The workspace is personalized — settings, dotfiles, and secrets | the https://containers.dev[Dev Container] spec (`remoteEnv`/`containerEnv`, `secrets`), the VS Code settings + https://code.visualstudio.com/docs/editor/settings-sync[Settings Sync] surfaces, and the Codespaces/Gitpod *dotfiles* convention | the operator's settings, dotfiles, and secrets are κ-addressed content scoped to their identity (`CC-1`/`CC-12`, Law L3 — held in the store, synced by `KappaSync`, not a server account); on entry holospaces applies them — settings to the workbench, dotfiles + `remoteEnv`/secrets into the devcontainer OS (`CC-14`/`CC-15`) — so the operator's environment follows them across peers (Q6), as a Codespace/Gitpod carries dotfiles + secrets | planned (`vv/suites/cc23-personalization`)
| CC-24 | The devcontainer signs in to GitHub and other services over the network | the https://datatracker.ietf.org/doc/html/rfc8628[OAuth 2.0 Device Authorization Grant (RFC 8628)] — the secretless flow `gh auth login` uses and that fits a Pages deployment — with a hermetic GitHub-shaped device-flow server as the differential oracle | holospaces does *not* intermediate auth (ADR-017): it provides the network (`CC-16`) and the devcontainer's tools (and any extension's authentication provider) sign in over it with the *service's own* OAuth flow; the devcontainer requests a device + user code, polls through `authorization_pending`, and obtains an access token — **the token lives in the devcontainer, never in holospaces** (content-blind; Laws L1/L3). Service-agnostic (GitHub, GitLab, a registry, a cloud SDK — each with its own provider) | live (`vv/suites/cc24-auth`)
|===

The capability set above is the **Codespaces/Gitpod parity** target (ADR-016): a repository becomes a ready, real development environment with the authentic VS Code workbench, extensions and their integrations, language intelligence and debugging, port-forwarded app previews, dev-container Features and lifecycle, and personalization — realized over the substrate with no server. ADR-016 maps each capability to its conformance row and substrate primitive so the model has no gaps against Codespaces/Gitpod.

The `CS` rows are witnessed green by every documentation build. The `CC` rows are each witnessed by a suite in `vv/suites/` that validates the component against its external authority — never against itself — by composing the https://github.com/Hologram-Technologies/hologram[hologram] substrate (consumed by reference, ADR-006), at the rev pinned in the workspace `Cargo.toml`. Adding a component without satisfying its `CC` row leaves holospaces incomplete by definition.

=== Quality gates and test tiers

The quality commitments above are enforced from the first commit by continuous integration (`.github/workflows/ci.yml`), which runs on every change:

* *V&V* — `vv/run.sh`: CS-1..CS-6 specification conformance (V1–V8) and the CC-1..CC-11 component suites (`vv/suites/`), each against its external authority.
* *Format* — `cargo fmt --check`.
* *Lints* — `cargo clippy` with warnings denied; the workspace forbids `unsafe_code` and warns on `missing_docs`.
* *Docs* — `cargo doc` with warnings denied (no broken links; every public item documented).
* *Unit* — `cargo test --lib`: a building block in isolation.
* *Integration* — `cargo test --test integration`: building blocks composed.
* *End-to-end* — `cargo test --test e2e`: whole operator flows (over the real substrate runtime and a real content-addressed gateway).
* *Portability* — the peer core builds for every supported environment: native, browser (`--target wasm32-unknown-unknown`), and bare-metal (`--no-default-features --target thumbv7em-none-eabi`, `no_std`). The same holospace κ boots on any peer (Chapter 7; quality goal Q6).
* *Browser* — `scripts/browser-manager-test.sh`: the Hologram Platform Manager (the browser peer) runs in headless Chromium (Playwright) — sign in, provision (both compute forms), view, resolve + verify by re-derivation (Law L5), a live `.holo`-engine differential (the executor compiled to wasm yields a κ identical to native — `CC-2`, RT2), and the execution surface — a κ-addressed Wasm code module is validated against the host-ABI surface, provisioned, **and booted as a container in-browser** through hologram's `wasmi` interpreter engine (suspended to a κ snapshot, resumed, terminated — `CC-6`, ADR-008's contract generalized by ADR-009; an ambient import is refused). It is deployed to GitHub Pages by `.github/workflows/pages.yml` (the cold-start, untrusted gateway of Chapter 6).
* *Execution surface* — `cargo test --test cc6_execution_surface`: a κ-addressed Wasm code module (the surface the system emulator binds; ADR-008's contract, generalized by ADR-009) is validated against the WebAssembly specification and the substrate host-ABI surface, then booted, suspended, resumed, and migrated on the real `ContainerRuntime` over **both** engines — the native Wasmtime JIT and hologram's `wasmi` interpreter (the browser + bare-metal engine) — yielding the same holospace κ on each (Q6). A devcontainer holospace boots the code its config selects (`cargo test --test e2e`). The RT1 surface (`CC-6`).

Every `CC` suite and test tier witnesses its component against its external authority on every change; each runs from the first commit.
