# Imported validation artifacts — provenance

Every artifact holospaces is validated against is external and authoritative, imported and pinned. This file is the authoritative record of *what* is imported, *from where*, at *which pin*, and *how it is verified*. See the Conformance catalog in [arc42 chapter 10](../docs/src/arc42/adoc/10_quality_requirements.adoc) for which invariant each artifact validates.

## Live (specification conformance, `CS-*`)

These drive the documentation build (validators V1–V8) today.

| Artifact | Authority | Source | Pin | Verified by |
|---|---|---|---|---|
| arc42 template | arc42 (https://arc42.org) | `github.com/arc42/arc42-template` | SHA in `docs/tools/arc42-template-pin.txt` | checked out at the pin by `docs/scripts/install-tools.sh`; structural superset enforced by V1 |
| arc42-generator | arc42 | `github.com/arc42/arc42-generator` | submodule `docs/vendor/arc42-generator` (pinned commit) | git submodule pin; runs in V2 |
| OPM OPL grammar | ISO/PAS 19450:2015 (OPM) | transcribed EBNF, `docs/tools/iso-19450-opl.ebnf` | in-repo, pinned | V6 parses every `opl.txt` against it; V7 checks OPD↔OPL coherence |
| ISO 15288 process catalog | ISO/IEC/IEEE 15288 | process list, `docs/tools/iso-15288-processes.txt` | in-repo, pinned | V8 superset check |
| cmark-gfm | CommonMark / GFM | `github.com/github/cmark-gfm` | tag in `docs/tools/versions.txt`; sha256 in `docs/tools/checksums.txt` | built at the pinned tag by `install-tools.sh`; runs in V4 |
| github-markup | GitHub rendering | RubyGem (`docs/Gemfile.lock`) | locked version | runs in V5 |
| Structurizr | C4 (Structurizr DSL) | `download.structurizr.com` | version in `versions.txt`; sha256 in `checksums.txt` | checksum-verified on download by `install-tools.sh`; runs in V3 |

## Live (component conformance, `CC-*`)

Every component is implemented as a thin composition of the [hologram](https://github.com/Hologram-Technologies/hologram) substrate, consumed by reference (ADR-006) at the pinned rev `18f553d8578997ce32e7b653786a0bcf9b09a2c0` (git dependency, see the root `Cargo.toml`). Each `CC-*` row is witnessed against an external authority by its suite in `vv/suites/`, run by `vv/run.sh`.

Every artifact is external/authoritative — imported verbatim and pinned, never hand-authored. (This mirrors hologram's own conformance discipline, e.g. its `AS` class validates the σ-axis against the reference `blake3` crate, not against a vendored vector file.)

| Row | Authority | Source / pin | Verified by |
|---|---|---|---|
| `CC-1` κ-labels | the **reference implementations** of the σ-axis standards — the `blake3`, `sha2`, `sha3` crates (canonical BLAKE3 / FIPS 180-4 / FIPS 202 / Keccak), each tested against the published vectors upstream | the crates pinned in `Cargo.lock` (no vendored vectors) | `cc1-kappa-addressing.sh` → `tests/cc1_kappa_kat.rs`: holospaces' κ-label digest equals the reference implementation byte-for-byte across all five axes and across chunk/subtree boundaries (hologram `AS` pattern); determinism, single-bit sensitivity, and re-derivation (Law L5) |
| `CC-2` `.holo` engine | the native hologram `.holo` executor (`hologram-exec`) as oracle | `Hologram-Technologies/hologram` (pinned rev) | `cc2-holo-engine.sh` → `tests/cc2_holo_engine.rs`: identical `.holo` yields identical κ across independent builds and across the byte- and address-boundary surfaces (determinism + content-addressing). **Live browser differential:** `scripts/browser-manager-test.sh` (CI `browser` job) runs the same `.holo` through the executor compiled to wasm in headless Chromium and asserts an identical output κ to native — the browser engine equals the native one (arc42 ch.11 RT2, realized) |
| `CC-3` peer storage | the hologram substrate conformance battery (TCK) | `hologram-substrate-tck` (pinned rev) | `cc3-substrate-tck.sh` → `tests/cc3_substrate_tck.rs`: `store_battery` run against the stores holospaces resolves through (`hologram-store-mem`, `hologram-store-native`) |
| `CC-4` devcontainer holospace | the **published Dev Container JSON Schema** + **real authoritative `devcontainer.json` configs** | `vv/artifacts/cc4/devContainer.base.schema.json` (`devcontainers/spec@c95ffeed`, sha256 `a0883c04…`); `*.devcontainer.json` (`devcontainers/templates@b3645066`); pins in `vv/artifacts/cc4/SOURCE.txt` | `cc4-devcontainer.sh` → `tests/cc4_devcontainer.rs`: every real template config validates against the schema (the schema is the judge) and the ingestor accepts it; the schema rejects a non-conformant config; same source ⇒ same κ (Q4). The repo config is ingested with its declared image source, or as the documented default-image superset if it declares none |
| `CC-5` Wasm modules | the **WebAssembly specification's own `test/core` conformance suite** | `vv/artifacts/cc5/{func,binary}.wast` imported verbatim from `WebAssembly/spec@93c7fab` (commit in `vv/artifacts/cc5/SOURCE-COMMIT.txt`) | `cc5-wasm.sh` → `tests/cc5_wasm.rs`: holospaces' validator agrees with the spec's own `module` / `assert_invalid` / `assert_malformed` directives; the substrate's closed host surface (spec §4.4) refuses non-`hologram` imports |
| `CC-6` execution surface | the **WebAssembly specification** (validity of the recompiled userland + its closed host-ABI surface) and the **hologram `ContainerRuntime` contract** (the real runtime as the execution oracle), on **both** engines | the WebAssembly spec (as `CC-5`); `Hologram-Technologies/hologram` `hologram-runtime` + `hologram-runtime-wasmtime` (native JIT) + `hologram-runtime-bare` (`wasmi` interpreter — the browser + bare-metal engine), pinned rev | `cc6-execution-surface.sh` → `tests/cc6_execution_surface.rs`: a κ-addressed Wasm-recompiled userland (the second compute form, ADR-008) validates against the spec + host-ABI surface (`surface::validate_userland` — ambient/WASI imports and missing container-ABI exports are refused), then boots, suspends to a κ snapshot, resumes, and migrates on a real `Runtime` over **both** `WasmtimeEngine` and `BareMetalEngine` (`wasmi`), yielding the same holospace κ (Q6/QS1). `tests/e2e.rs` boots a devcontainer holospace's selected userland; `scripts/browser-manager-test.sh` boots a userland container in headless Chromium via the interpreter engine. No located OCI image (Laws L1/L2/L4) |
| `CC-7` κ-disk | a **real on-disk ext4 filesystem image** produced by **e2fsprogs** (the canonical Linux ext2/3/4 implementation), with **`debugfs`** (e2fsprogs' own ext reader) as an *independent* read-back oracle | `vv/artifacts/cc7/rootfs.ext4` minted by `mke2fs -d` (reproducible — fixed UUID / hash seed / `E2FSPROGS_FAKE_TIME`; sha256 in `rootfs.ext4.sha256`); command + e2fsprogs version in `vv/artifacts/cc7/SOURCE.txt` | `cc7-kdisk.sh` → `tests/cc7_kdisk.rs`: the image is written through the `KappaStore`-backed `BlockDevice` (`disk::KappaDisk`) sector-by-sector and read back **byte-identically**; `debugfs` then reads the real files out of the round-tripped image (the filesystem survived intact); identical sectors store once (dedup, L2/L3) and the image κ is reproducible (L1) |
| `CC-8` import-and-run | the **hologram driver-import contract** (verify-by-re-derivation on import) and the **hologram `ContainerRuntime` capability contract** (the real Wasmtime runtime as the enforcement oracle) | `Hologram-Technologies/hologram` `hologram-runtime` + `hologram-runtime-wasmtime`, pinned rev (no vendored artifact — the runtime *is* the authority, the program's deterministic output κ is its own oracle) | `cc8-import-run.sh` → `tests/cc8_import_run.rs`: a program is imported by κ (`boot::import` over `get_with_fetch`) and a **forged** import — bytes that do not re-derive to the κ — is refused (Law L5); the verified program then runs on the real runtime doing real `storage_put`/`storage_get`, and an over-**quota** put and a get of a κ outside the **storage roots** are denied by the runtime (capability-scoped I/O), mirroring hologram's `driver_import` / `resource_limits` witnesses |
| `CC-10` devcontainer ingestion | the **OCI image specification** (a real OCI image-layout — every blob named by its `sha256` digest, which *is* a κ-label) and the **Dev Container specification** (the repository config) | `vv/artifacts/cc10/image` — a real OCI image-layout produced by **BuildKit** (a conforming OCI implementation; `docker buildx --output type=oci`, reproducible — `SOURCE_DATE_EPOCH` + `rewrite-timestamp`); `vv/artifacts/cc10/devcontainer.json` (Dev Container, pin via `vv/artifacts/cc4/SOURCE.txt`); commands + versions in `vv/artifacts/cc10/SOURCE.txt` | `cc10-ingestion.sh` → `tests/cc10_ingestion.rs`: `oci::ingest_image` walks index → manifest → config + layers and verifies every blob by re-derivation against its OCI `sha256` digest (`verify_kappa_axis`; a forged blob is refused — Law L5); `oci::devcontainer_source_identity` validates the `devcontainer.json` (`CC-4`) and binds it to the ingested image into a reproducible κ (same source ⇒ same holospace κ, Law L1); the ingested layer round-trips as `CC-7` κ-disk content. The booted behaviour of the image is `CC-9` |
| `CC-11` workspace projection | the **substrate store + the reference σ-axis hashes** (`CC-3`/`CC-1`) for the Editor/FS surface, and a **real, running Linux terminal + `qemu-system-riscv64`** (the reference RISC-V machine, the differential oracle) for the Terminal surface | `vv/artifacts/cc11/Image.gz` — the CC-9 RV64GC Linux **6.6** kernel with an *interactive* PID-1 (`shell.c`, a tiny real shell; non-echoing tty so its output is deterministic), built reproducibly; `input.txt` (the operator's lines) + `expected-session.txt` (the QEMU-captured terminal session); provenance + reproducible build in `cc11/SOURCE.txt`, `sha256` in `cc11/cc11.sha256` | `cc11-workspace.sh` → `tests/cc11_workspace.rs`: the `projection::Workspace` **Editor/FS** surface reads environment content by κ (verified by re-derivation, L5) and an edit advances that κ (a new content identity, L1); the **Terminal** surface boots the interactive kernel, then types the operator's lines — each published as a canonical event (`Intent`, L1/L2) that **advances the running holospace's κ snapshot** — and the rendered terminal is byte-identical to `qemu-system-riscv64` on the same image (`expected-session.txt`, re-derived live whenever QEMU is present). The projection holds no state of its own (Law L3) |
| `CC-12` management console | the **self-sovereign identity** (`CC-1` σ-axis hashes) and the validated lifecycle paths the console composes — provision (`CC-4`), enter→boot (`CC-9`), the workspace (`CC-11`) | no new artifact — the console is the Platform Manager projection over the witnessed paths; the browser peer (`crates/holospaces-web`) built by `scripts/browser-manager-test.sh` | `cc12-manager-console.sh` → `tests/cc12_manager_console.rs`: a content-addressed operator identity and reproducible provisioning κ (the `(repo, reference, config, arch)` tuple is the holospace identity, Law L1; the console holds no state of its own, Law L3 — the holospaces + roster are κ in the store). **Live browser lifecycle:** `scripts/browser-manager-test.sh` (CI `browser` job) drives `crates/holospaces-web/web/manager-test.mjs` (Playwright/Chromium) — sign in to a content-addressed identity, the console lists + organizes the operator's holospaces with status, and each lifecycle action (provision · enter · suspend · resume · terminate) runs through the real browser peer |
| `CC-14` VirtIO block boot | the **OASIS VirtIO v1.2 specification** (`virtio-mmio` + `virtio-blk`) and the **RISC-V PLIC**, a **real OCI image** (`CC-10`) assembled into an **ext4** `CC-7` κ-disk by the in-crate Layer Assembler, and **`qemu-system-riscv64`** as the differential oracle | `vv/artifacts/cc14/kernel/Image.gz` — a real, unmodified Linux **6.6** RV64GC kernel built (from `defconfig`, the IRQ-correct base) with `VIRTIO_MMIO`+`VIRTIO_BLK`+`EXT4` and an SBI console, reproducibly (`kernel.config` committed); `kernel/holospaces-virtio.dtb` (PLIC + virtio-mmio nodes, QEMU-`virt` layout); `image/` — a real OCI image-layout whose `tar+gzip` layer is a freestanding RV64 `/init` (`image/init.S`); `expected.txt` (the userspace marker QEMU prints); pins + commands in `cc14/SOURCE.txt` | `cc14-virtio-block.sh` → `tests/cc14_assembly.rs` (the OCI layer assembles into an ext4 that **e2fsprogs `e2fsck` finds clean** + **`debugfs` reads back byte-identical**, reproducible) **and** `tests/cc14_virtio_block.rs` (the full chain: ingest the OCI image (`CC-10`) → assemble its layers into an ext4 rootfs (the Layer Assembler, Law L4) → a real Linux kernel boots on the emulator and **mounts that rootfs over the emulator's `virtio-blk`** (PLIC-routed virtio-mmio, the descriptor/used-ring protocol) and runs real userspace, **behaviour matching `qemu-system-riscv64`** on the same kernel + rootfs; the boot is byte-for-byte reproducible, Law L1) |
| `CC-15` shared workspace FS | the **9P2000.L protocol** over a spec-conformant **`virtio-9p`** device (the VirtIO spec), with **`qemu-system-riscv64`**'s own 9p server as the differential oracle | `vv/artifacts/cc15/image/` — a real OCI image whose `tar+gzip` layer is `/init` = a freestanding RV64 program (`init9p.c`, no libc) that mounts the workspace over `virtio-9p`, reads `from-holospaces.txt`, and writes `from-guest.txt`; the kernel is reused from `CC-14` (its defconfig base has `NET_9P_VIRTIO`+`9P_FS`); pins + the QEMU oracle command in `cc15/SOURCE.txt` | `cc15-virtio-9p.sh` → `tests/cc15_workspace.rs`: the emulator serves a workspace holospaces seeds; a real Linux **mounts it over `virtio-9p`** and reads the file holospaces placed there (`READ:…`), and **holospaces reads back the file the OS wrote** over 9P (`from-guest.txt` = `GUEST-WROTE-THIS`) — one shared content (Law L1); behaviour matches the QEMU 9p server on the same kernel + init |
| `CC-20` import boundary | the **OCI distribution specification** (registry pull — manifest + blobs by digest), an HTTP(S) **git-host archive** (the repository content at a reference), and the **Dev Container** default (a repository with no devcontainer) — the internet is an *untrusted gateway* (ADR-013), every byte *verified by re-derivation* (a registry digest *is* a κ; Law L5) | no committed network artifact (the import is a live transport); the hermetic witness *re-serves* the pinned `CC-14` OCI image + a generated repository archive over a localhost OCI-distribution server; the `net` feature links `ureq` + rustls | `cc20-import.sh` → `tests/cc20_import.rs` (the `net` feature): **hermetically**, a localhost server serves a repository archive (with a `devcontainer.json`) + the pinned OCI image over the real `/v2/.../manifests` + `/v2/.../blobs` endpoints; `import::import_and_assemble` fetches the archive by URL, reads its config, **pulls the image over the OCI distribution protocol and verifies every blob by re-derivation** (`CC-10`), assembles the rootfs (`CC-7`), and a real Linux **boots and mounts it** (`CC-14`); a repository with no `devcontainer.json` falls back to the default image. A `#[ignore]` live test pulls the real default image from Docker Hub (token auth + multi-arch `riscv64` select) — real-world interop, network-gated |
| `CC-16` internet access | the **OASIS VirtIO v1.2 specification** (`virtio-net`, device id 1; the two-virtqueue RX/TX transport with the 12-byte `virtio_net_hdr_v1`) and the **TCP/IP standards** — **RFC 826** (ARP), **RFC 791** (IPv4), **RFC 768** (UDP), **RFC 2131** (DHCP), **RFC 9293** (TCP) — with **`qemu-system-riscv64`**'s own user-mode (slirp) network as the differential oracle; the egress is a real host socket (`std::net`) | `vv/artifacts/cc16/kernel/Image.gz` — a real, unmodified Linux **6.6** RV64GC kernel built (from `defconfig`) with `VIRTIO_NET` + the INET IPv4 stack + the kernel's built-in DHCP autoconfiguration (`IP_PNP`/`IP_PNP_DHCP`, used via `ip=dhcp`) + virtio-blk/9p + an SBI console, reproducibly (`kernel.config` committed); `image/` — a real OCI image-layout whose `tar+gzip` layer is `/init` = a freestanding RV64 program (`initnet.c`, no libc) that `socket`/`connect`s a TCP stream, sends an HTTP request, and reads the response; pins + the QEMU oracle command in `cc16/SOURCE.txt` | `cc16-network.sh` → `tests/cc16_network.rs`: a real Linux **boots on the emulator, autoconfigures its `virtio-net` interface with DHCP** against the in-emulator **userspace TCP/IP NAT** (ARP for the subnet + a DHCP server leasing `10.0.2.15`), its userspace **opens a TCP connection through the NAT's guest-facing state machine** (SYN→SYN-ACK→data→FIN, with checksummed IPv4/TCP framing) and **completes an HTTP exchange to a real host server over the native egress** (a `std::net` socket, port-forwarded as QEMU's `guestfwd` does) — `NET-CONNECTED` · `NET-RECV: HTTP/1.0 200 OK …` · `NET-DONE`, behaviour matching `qemu-system-riscv64`'s slirp on the same kernel + init. The NAT is `no_std` and **compiles to `wasm32`** with the peer core; only the egress seam differs in the browser. **Also witnessed in the browser** (`crates/holospaces-web/web/devcontainer-net-test.mjs`, Chromium via Playwright): the same wasm peer boots the net devcontainer in a tab, does DHCP over `virtio-net`, and completes the HTTP exchange with the egress **tunnelled over a WebSocket to a content-blind relay** (`web/relay.mjs`, a pure-Node RFC 6455 TCP-over-WebSocket proxy — the egress gateway; ADR-014) that opens the real socket to the host server |
| `CC-17` VS Code web workbench (Phase 1) | the **real VS Code web workbench** — the same compilation that powers https://vscode.dev[vscode.dev] / github.dev (ADR-012), pinned as **`vscode-web@1.91.1`** (npm integrity `sha512-kzrbnxx…ky3nD8Q==`) — *not* a reconstruction from Monaco + xterm (that is `CC-13`); the workbench is **imported by pin and verified by re-derivation through the substrate** before it loads (Law L5) | `vv/artifacts/cc17/vendor.sha256` — the sha256 of the workbench's *executable core* (the AMD loader, `webPackagePaths`, the `workbench.web.main` bundle + its nls, and the `workbench.js` bootstrap); `cc17/SOURCE.txt` pins the package + integrity + the phase plan. The 60 MB build is fetched by the pin (as the browser tests fetch Playwright), not committed; the manifest is its κ-identity | `cc17-vscode-workbench.sh` → `crates/holospaces-web/web/vscode-workbench-test.mjs` (Chromium/Playwright): the pinned `vscode-web` is assembled, its executable core is **re-derived against `vendor.sha256` and a forged byte is refused** (L5), then served as the holospaces Workspace Projection tab — and the **real VS Code web workbench boots to its authentic UI** (the `.monaco-workbench`, the activity bar, the "holospaces VS Code" title). Phase 2 (a `FileSystemProvider` over the `virtio-9p` workspace `CC-15` + the terminal over the console `CC-11`) and Phase 3 (the remote extension host in the devcontainer OS, ADR-015 — the base for `CC-18`/`CC-19`) follow |
| `CC-17` editor filesystem (Phase 2 foundation) | the **9P2000.L** shared workspace (`CC-15`) as the editor's `FileSystemProvider` backing — the κ-addressed content the editor and the running OS *share* (Law L1), *not* a separate store (ADR-012/015); the κ identity is `CC-1`'s blake3, the OS-visible sharing oracle is `qemu-system-riscv64`'s 9p server | no new artifact — the editor-side API (`Emulator::workspace_list`/`workspace_write`/`workspace_file`, exposed on the wasm peer as `Workspace::ws_list`/`ws_read`/`ws_write`) over the `CC-15` `virtio-9p` workspace; reuses the `CC-15` OCI init | `cc17-workspace-fs.sh` → `tests/cc17_workspace_fs.rs`: a fast witness (the editor lists/writes/reads the workspace; a file's identity is its content κ; a re-write updates in place and advances the κ — Law L1/L2) **and** an `#[ignore]` real-OS boot witness (**a file the *editor* writes is read by the real Linux over `virtio-9p`** — one shared content, Law L1). The browser wiring (a service worker bridging the workbench's web-extension `FileSystemProvider` to these wasm-peer methods) sits on top of this substrate API |
| `CC-17` workbench filesystem (Phase 2) | the **real VS Code web workbench** driven the *supported* way — **`@vscode/test-web`** (Microsoft's own web-extension test harness), whose built-in `FileSystemProvider` serves a workspace to the workbench exactly as https://vscode.dev[vscode.dev] / github.dev / Codespaces do; *no* hand-rolled embedder, service worker, or extension (ADR-012/015) | none committed — the pinned `@vscode/test-web@0.0.80` (npm) serves the build; `--browser none` serves only and the witness drives the **system Chromium** (the harness's bundled launcher is unavailable in this sandbox) | `cc17-workbench-fs.sh` → `crates/holospaces-web/web/vscode-workbench-fs-test.mjs` (Chromium/Playwright): a holospace workspace is served to the **real VS Code web workbench** via `@vscode/test-web`'s built-in `FileSystemProvider`; the genuine workbench loads (activity bar, explorer) and **its `readDirectory` reaches the real editor** — the holospace's workspace is presented in the authentic workbench through the supported mechanism (verified visually + by the explorer tree). The editor↔workspace *content* path (content by κ, a file the editor writes read by the real OS over `virtio-9p`) is witnessed by `cc17-workspace-fs.sh` |
| `CC-17` workbench over the running holospace (Phase 3) | the **real VS Code web workbench** bound to the **running holospace**, served **statically** (no server) — the substrate-native realization of ADR-012/ADR-015's web extension-host model. holospaces is the gateway: it serves Microsoft's κ-verified `vscode-web` executable core **byte-identical** (Law L5) and *composes* it as its own content with VS Code's supported web-embedding bootstrap (`create()`) + the `holospace-fs` builtin extension; the authority is the real `vscode-web` build + its published `FileSystemProvider`/terminal APIs and the holospace primitives (`CC-15`/`CC-11`) | `crates/holospaces-web/web/builtin-extensions/holospace-fs/` — the holospace bridge extension (browser entry; boots the holospace `Workspace` wasm peer **in the extension-host worker** and registers a `FileSystemProvider` over `ws_list`/`ws_read`/`ws_write` (`CC-15`) + a `Pseudoterminal` over `terminal()`/`type_line()` (`CC-11`)); `build-workbench.mjs` composes the workbench (the supported `create()` bootstrap from pinned `@vscode/test-web@0.0.80`; the open gallery, Open VSX); the pinned `vscode-web@1.91.1` core (κ in `cc17/vendor.sha256`); reuses the `CC-14` kernel + layer the browser peer boots | `cc17-workbench-holospace.sh` → `crates/holospaces-web/web/vscode-workbench-holospace-test.mjs` (Chromium/Playwright): composes the workbench exactly as the deploy does and asserts — the executable core **re-derives to its pinned κ** (L5); the **real workbench loads** (activity bar, not Monaco); the `holospace-fs` extension **boots the devcontainer in the worker and mounts its `virtio-9p` workspace into the real editor** (`CC-15` — a file the holospace holds is in the file tree); the **editor reads that file's content over the live workspace** (`CC-11`/`CC-15`); and the workbench is **wired to the open gallery (Open VSX)**, not the MS Marketplace (`CC-19`). This is the *deployed* Workspace Projection (`.github/workflows/pages.yml` runs `build-workbench.mjs`; the Manager enters `workbench.html`; the `CC-13` Monaco `workspace.html` is the documented fallback) |
| `CC-19` extensions | the **VS Code extension host** running a real web extension in the real workbench — driven the *supported* way via **`@vscode/test-web --extensionDevelopmentPath`** (Microsoft's own harness does the extension-host + service-worker wiring; the same model Codespaces / vscode.dev use), *no* hand-rolled embedder or activation hack (ADR-012/015) — plus the **open marketplace** ([Open VSX](https://open-vsx.org), the gallery vscode.dev / Gitpod / code-server use; the MS Marketplace ToS forbids non-MS products) wired as the workbench's `extensionsGallery` so *arbitrary* extensions install with no Microsoft/GitHub lock-in | `crates/holospaces-web/web/extensions/holospace-demo/` — a real web extension (`browser` entry; contributes a command + a status-bar item on activate); pinned `@vscode/test-web@0.0.80` serves it (`--browser none`; our system Chromium drives). The workbench `productConfiguration` sets `extensionsGallery.serviceUrl` to `https://open-vsx.org/vscode/gallery` | `cc19-extensions.sh` → `crates/holospaces-web/web/vscode-extension-test.mjs` (Chromium/Playwright): the extension is served to the **real VS Code web workbench** and **activates in its extension host** — its status-bar contribution appears in the genuine workbench (deterministic; the palette command surfaced best-effort). And `vscode-workbench-test.mjs` asserts the served workbench is **wired to the open gallery** (`product.json` `extensionsGallery.serviceUrl` is Open VSX, *not* `marketplace.visualstudio.com`) — arbitrary extensions install, no lock-in; the base set comes from `devcontainer.json` `customizations.vscode.extensions` (`CC-4`). This host is the prerequisite language servers (`CC-18`) and DAP debugging build on |
| `CC-48` substrate-native extension host activates an arbitrary Node-only extension | the **VS Code extension API** + the **Open VSX** open gallery; an **unmodified, Node-only** marketplace extension (`package.json` `main`, **no** `browser` entrypoint — so it *cannot* run in vscode-web's web ext host) as the subject. The substrate-native host runs the extension on the browser peer's **own JS engine** (native exec, in the tab), backed by the holospace's own filesystem (`CC-15`), terminal (`CC-11`), and network (`CC-16`) — never `openvscode-server` in the emulated guest (the interpreter wall), never the web ext host (that is `CC-19`), no Node on the host, no deployment outside the holospace | `vv/artifacts/cc48/editorconfig.editorconfig-0.18.2.vsix` — the **unmodified Open VSX `.vsix`** of `editorconfig.editorconfig@0.18.2` (a genuinely Node-only extension whose `activate()` exercises the real Node surface: a bare `require('editorconfig')` resolved from the bundled `node_modules`, and a load-time `fs.readFileSync` of `@one-ini`'s bundled `.wasm`); pinned + reproduce in `cc48/SOURCE.txt`, `cc48/build.sh`, `cc48/cc48.sha256`. The witness pins it by sha256 and serves it to the in-browser host by intercepting Open VSX, so the gate is hermetic; the deployed `holospace-fs` resolves the real Open VSX registry, unchanged | `vv/suites/cc48-ext-host.sh` → `crates/holospaces-web/web/ext-host-test.mjs` (Chromium/Playwright): the deployed workbench loads κ-verified (`CC-17`), the `holospace-fs` builtin brings up the **substrate-native (wasm-exec) Node-API extension host** (a CommonJS module host + Node API surface — `path`/`os`/`fs`(over `CC-15`)/`events`/`process`/… — with the `vscode` API passed through to the real workbench), downloads + unzips the committed `.vsix`, and runs the extension's `activate()` on the browser peer's own engine. The committed `.vsix` **re-derives to its pinned sha256** (Law L5), the subject is verified **Node-only** (its package.json has `main`, no `browser`), and the host publishes `HOLOSPACE-NODE-EXTHOST-LIVE: editorconfig.editorconfig` (a status-bar item, in the DOM) **only after a genuine `activate()` returns** — observed in the real workbench. No emulated-guest server, no Node on the host, no deployment outside the holospace (Law L4) |
| `CC-24` external-service auth | the **OAuth 2.0 Device Authorization Grant (RFC 8628)** — the secretless flow `gh auth login` uses (no client secret, no redirect-URI backend → it works from a static Pages deployment); holospaces does *not* broker auth, it provides the `CC-16` network and the devcontainer signs in over it (ADR-017); the oracle is a hermetic GitHub-shaped device-flow server | `vv/artifacts/cc24/image/` — a real OCI image whose `tar+gzip` layer is `/init` = a freestanding RV64 program (`initauth.c`, no libc) that POSTs the device-code request, parses the `device_code`, and polls the token endpoint over its `virtio-net` interface; reuses the `CC-16` net kernel | `cc24-auth.sh` → `tests/cc24_auth.rs`: a real Linux **boots, brings up `virtio-net` (DHCP), and completes the GitHub device flow over the holospaces network** (the userspace NAT + native egress, port-forwarded to the hermetic RFC 8628 server as `gh` would reach `github.com`) — device-code request (`DEVICE-CODE-OK`) → `authorization_pending` poll (`POLL-PENDING`) → **access token obtained** (`AUTH-OK:gho_…`), the token held *in the devcontainer* (Laws L1/L3). The same flow signs in to any service |
| `CC-25` Dev Container Features | the **Dev Container Features specification** (the feature artifact format — an OCI artifact carrying `devcontainer-feature.json` + `install.sh` — and the install contract: `install.sh` runs in the container with the declared options as environment, before the lifecycle) + the **`ext4` on-disk format** (e2fsprogs as the oracle), with **`qemu-system-riscv64`** as the differential runtime oracle | `vv/artifacts/cc25/feature/` — a real OCI artifact whose `tar+gzip` layer carries a spec-conformant feature: `devcontainer-feature.json` (id `holospace-demo`, a `version` option) + `install.sh` (echoes `FEATURE-INSTALLED:${VERSION}`, writes `/usr/local/holospace-demo/version` — a real, busybox-runnable install); the source files are kept at `cc25/{install.sh,devcontainer-feature.json}`; reuses the **CC-22 BusyBox** shell base + the **CC-14** kernel; pins in `cc25/SOURCE.txt` | `cc25-features.sh` → `tests/cc25_features.rs`: (1) the config's `features` are **parsed (`CC-4`) and honoured** — the generated `/init` runs the feature's `install.sh` with the option as uppercased env (`VERSION='2.0'`) **before** the lifecycle (spec order); the feature is **imported by κ** (`assembly::extract_layer_files` over the ingested artifact) and its files are injected into the assembled `ext4` rootfs — `e2fsck` clean, `debugfs` reads `/opt/holospaces/features/0/install.sh` + `devcontainer-feature.json` back; (2) **holospaces' own emulator** boots the rootfs and the feature's `install.sh` runs in the OS — `FEATURE-INSTALLED:2.0` (the option applied) appears, before the lifecycle command `CC25-POSTCREATE:after-features`; (3) `qemu-system-riscv64` produces the same markers. *Not parse-and-drop — the declared feature is applied* |
| `CC-26` Dockerfile build | the **Dockerfile reference** (`FROM`/`ARG`/`ENV`/`WORKDIR`/`COPY`/`RUN` semantics; line continuations; `$VAR`/`${VAR}` substitution in the substituted instructions; `RUN` expanded by the shell at run time) + the **Dev Container `build`** + the **`ext4` format** (e2fsprogs), with **`qemu-system-riscv64`** as the differential runtime oracle | `vv/artifacts/cc26/Dockerfile` + `setup.sh` — the witness's Dockerfile and its build context (the `COPY` source); the `FROM` base is the **CC-22 BusyBox** image, the **CC-14** kernel is reused; the build is synthesized in the witness, kept here for provenance; pins in `cc26/SOURCE.txt` | `cc26-build.sh` → `tests/cc26_build.rs` + the `holospaces::dockerfile` unit tests: the parser resolves `FROM` with the `ARG` (and `build.args` override the `ARG` default), `ENV`/`WORKDIR`/`COPY`, `RUN` (shell + exec form); **no `FROM` is an error and an unknown instruction is an explicit error — never a silent drop**; (1) the build `/init` runs the `RUN` steps with the `ENV` exported + the `WORKDIR` entered, the `COPY` source is injected into the assembled `ext4` rootfs at its destination, `FROM` is the pulled base — `e2fsck` clean, `debugfs` reads the `COPY`'d script back; (2) **holospaces' own emulator runs the build in the OS** — `BUILD-START`, `SETUP-RAN` (the `COPY`'d script ran), `BUILD-RAN:cc26` (the `RUN` expanded the `ENV`), `BUILD-DONE`; (3) `qemu-system-riscv64` produces the same markers. *No Docker daemon — the build runs on the emulator over the substrate* |
| `CC-27` Docker Compose | the **Compose specification** (`services.<name>.{image\|build}`) + the **Dev Container** `dockerComposeFile`/`service` | `vv/artifacts/cc27/repo.tar.gz` — a hermetic repository archive declaring a compose devcontainer (`.devcontainer/{devcontainer.json` with `dockerComposeFile`+`service`, `docker-compose.yml` with an `app` + a `db` service}); the compose file is kept at `cc27/docker-compose.yml`; pins in `cc27/SOURCE.txt` | `cc27-compose.sh` → `tests/cc27_compose.rs` + the `holospaces::compose` unit tests: `resolve_service` resolves a named service's `image`, a build service to a `Build` (context/dockerfile/args); a missing service is `ServiceNotFound`, multiple services with no selection is `AmbiguousService` — **never a silent default**; (import) from the repository archive holospaces finds the `devcontainer.json`, parses it to a `Compose` source (the file path + service retained), reads the compose file from the repository, and **resolves the `app` service's declared image source** (`holospaces/busybox:latest` — the image string) — not the `db` service, not the default; an undeclared service is refused. Provisioning from the resolved source then follows the `CC-20` pull / `CC-26` build paths; the `import.rs` routing of a compose service's `image`/`build` to those paths (shared `resolve_build`) exists but is not exercised end to end by this gated witness (it parses + resolves only — it does not pull a blob, build, or boot) |
| `CC-28` control-plane reconfiguration | the **substrate's content-addressing + sync contract** (a real loopback HTTP content-addressed gateway, *verify-on-receipt* — Law L5, the same model the `e2e` roster-sync witness uses) and, for the live network directive, a real host `TcpListener` bound on the running machine (the `CC-21` ingress dual); the **Codespaces/Gitpod (Ona) control-panel *reconfigure* UX** is the behavioural model (ADR-018) | *no committed artifact* — the configuration is content generated in the witness (a `Configuration` realization embedding the operator + target instance + directives across the four operation classes); the gateway is the real `hologram-net-http` loopback CAS server, reused from `e2e` | `cc28-reconfigure.sh` → `tests/cc28_reconfigure.rs` + the `holospaces::config` unit tests: (1) **the control plane reconfigures an instance over the substrate** — peer A (the panel) provisions an instance and `Manager::configure` publishes a κ-addressed `Configuration` spanning *lifecycle / storage / network / account-user*; peer B (the instance, same operator) `resolve_configuration`s it over the real HTTP-CAS gateway (verify-by-re-derivation, L5) and `apply`s it — the effective capabilities change (network fetch on, storage quota set), the lifecycle action and the port forward surface, the account grant is honoured; (2) the control plane **actually manages** the instance — `Manager::reconfigure` publishes the configuration *and drives the running session with it*: a `suspend` directive actually suspends it to a κ snapshot, `resume` resumes it, `terminate` ends it, and a storage directive replaces the effective capability set (a new κ, persisted, Law L1/L2) — the instance state changes, not just a published intent; (3) reconfiguration is **reproducible** (same configuration ⇒ same κ, L1) and **authority-scoped** (a configuration for a *different* instance is `WrongInstance`, from an *unauthorized* operator is `UnauthorizedOperator` — never silently applied, L3); (4) a `forwardPort` directive **modifies the running machine** — `Machine::forward_port` binds a real, reachable host listener *live*, no reboot (the `CC-21` ingress dual). The browser control panel (`Console::configure` + a `BroadcastChannel` control channel) carries the configuration to the running instance tab, which verifies it by re-derivation (L5) and applies it via `Workspace::reconfigure` — no server, no control-plane→instance RPC |
| `CC-21` port-forward / app preview | the OASIS **VirtIO** spec (`virtio-net`) + the TCP/IP standards, with **`qemu-system-riscv64`**'s user-mode `hostfwd` as the differential oracle; the *ingress dual* of the `CC-16` egress | `vv/artifacts/cc21/image/` — a real OCI image whose `tar+gzip` layer is `/init` = a freestanding RV64 TCP **server** (`initserv.c`, no libc) that binds `0.0.0.0:8080`, listens, accepts, and serves an HTTP response; reuses the `CC-16` net kernel | `cc21-port-forward.sh` → `tests/cc21_port_forward.rs`: a real Linux **boots and runs a TCP server**; holospaces forwards an ephemeral host port to the guest's `:8080` (the `StdIngress` — the NAT accepts the inbound connection and is the **active opener toward the guest**: it sends the SYN, the guest's SYN-ACK completes the handshake), and a **host client reaches the guest server through the forward** and reads `HELLO-FROM-GUEST-SERVER` — byte-identically, matching QEMU `hostfwd`. The forwarded ports come from `devcontainer.json`'s `forwardPorts` (`CC-4`); the browser peer substitutes a relay-served ingress route (ADR-014) |
| `CC-33` in-process bridge to a guest listener | the **TCP/IP** standards + the guest's own listening socket, over the same `10.0.2.0/24` userspace NAT as `qemu-system-riscv64` user networking; the *in-process* dual of the `CC-21` ingress (ADR-020) | *no new artifact* — the host-driven `net::LoopbackIngress`/`LoopbackHandle` (a `no_std`+alloc transport) attached via `Emulator::enable_loopback`, exposed on the wasm peer as `Workspace::dial_guest`/`guest_send`/`guest_recv`/`guest_close`; the guest server is the **`CC-21`** image (`initserv.c`, `:8080`) and the **`CC-16`** kernel, reused | `cc33-guest-bridge.sh` → (1) `crates/holospaces/src/emulator/net.rs` unit test `the_loopback_bridge_carries_both_byte_directions_to_the_right_side` — the transport plumbing (dial↔accept; host→guest `recv`; guest→host `send`; close), no OS; (2) `tests/cc33_guest_bridge.rs` (release; a real-OS boot) — the emulator boots the `CC-21` server with the loopback bridge enabled, the host `dial_guest(8080)`s it **in-process** (no host socket, no thread), writes `GET /` (host→guest) and reads `HELLO-FROM-GUEST-SERVER` (guest→host) byte-faithfully. This is the transport the VS Code remote extension host (`CC-34`, ADR-015/ADR-020) runs over: the same guest listener is reached by this in-process bridge in the browser and by a real socket on a native peer (Q6) |
| `CC-22` devcontainer lifecycle | the **Dev Container spec** (the lifecycle hooks `onCreateCommand`/`postCreateCommand`/… + their run order) + the **`ext4` on-disk format** (e2fsprogs `e2fsck`/`debugfs` as the oracle), with **`qemu-system-riscv64`** as the differential runtime oracle | `vv/artifacts/cc22/image/` — a real OCI image whose `tar+gzip` layer provides the shell: a statically-linked **BusyBox v1.37.0** for riscv64 (`/bin/busybox`, sha256 `644f74d3…`); reuses the `CC-14` kernel; pins in `cc22/SOURCE.txt` | `cc22-lifecycle.sh` → `tests/cc22_lifecycle.rs`: (1) the Boot Orchestrator **builds the lifecycle `/init` from the parsed config** (the commands in spec order, `remoteEnv` exported, hooks marked) — driven by the config, not hard-coded; (2) the `/init` is **injected into the assembled `ext4` rootfs** — `e2fsck` clean and `debugfs` reads `/init` **and** the 1.6 MB multi-extent `/bin/busybox` back byte-identically; (3) `qemu-system-riscv64` (the differential oracle) boots the byte-identical rootfs and produces the same markers — the realization is correct independent of the emulator; (4) **holospaces' own emulator runs the lifecycle commands** under a real **libc** (`busybox`) shell — `CC22-POSTCREATE:ready-on-entry` on its console, *ready on entry*, no QEMU in the loop. (Running the libc userland required one emulator-CPU fix — page-straddling accesses are now translated per-page; regression `page_straddling_access_translates_each_page`, see `cc22/SOURCE.txt`.) |
| `CC-18` language intelligence (LSP) | the **Language Server Protocol specification** — embodied by **`lsp-types`** (rust-analyzer's own LSP type crate, the spec's authoritative Rust embodiment, pinned in `Cargo.lock`) — *and* a **real language server running in the devcontainer OS** | `vv/artifacts/cc18/lsp-demo/` — the language server's *source* (a self-contained Rust crate built ON `lsp-types`: its advertised capabilities and every message body are `lsp-types` values; single-threaded; LSP base-protocol framing per spec), built **static for riscv64** (`riscv64gc-unknown-linux-gnu`, `crt-static`); committed in `vv/artifacts/cc18/image/` as a real OCI image whose `tar+gzip` layer provides `/usr/bin/lsp-demo` over the same **BusyBox v1.37.0** shell as `CC-22` (binary sha256 `cf8c862e…`); the server speaks LSP over **stdio** (default) *and* **TCP** (`--listen <port>` — the deployed delivery over the `CC-33` bridge, ADR-020); reuses the `CC-14` kernel (stdio) + the `CC-16` net kernel (bridge); repackage with `cc18/pack-image.py`; pins + build command in `cc18/SOURCE.txt` | `cc18-lsp.sh` → `tests/cc18_lsp.rs` (stdio) **and `tests/cc18_lsp_bridge.rs`** (the deployed bridge delivery: the server runs as a TCP service in the OS and a full LSP session — capabilities, hover, completion, definition, diagnostics, all validated through `lsp-types` — flows to it over the in-process substrate bridge, `CC-33`): (1) the workbench's LSP session is **spec-valid** (its params round-trip through `lsp-types`) and the real language-server binary + the session are present in the assembled `ext4` rootfs — `e2fsck` clean, `debugfs` reads the ~1 MB multi-extent `/usr/bin/lsp-demo` **and** `/session.lsp` back; (2) **holospaces' own emulator runs the language server in the OS and it speaks LSP** — its responses, deserialized through `lsp-types`, conform to the spec: `initialize` advertises hover/completion/definition capabilities, `hover` names the symbol `greet`, `completion` offers the document's identifiers, go-to-`definition` returns a `Location` at the definition (line 0), and `publishDiagnostics` reports the `TODO` (line 1). A minimal-but-real server witnesses the LSP channel + spec conformance end to end; a heavier server is the same channel, a larger binary. **And `crates/holospaces-web/web/lsp-test.mjs`** (Chromium) witnesses the *deployed browser* delivery: the deployed wasm peer boots the bridged devcontainer, the in-OS server listens, and the same full LSP session (capabilities, hover `greet`, completion, definition line 0, the TODO diagnostic) flows to it over the in-process bridge — real language intelligence in the tab, no Node |
| `CC-23` personalization | the **Dev Container spec** (`remoteEnv`/secrets as environment) + the **Codespaces/Gitpod dotfiles** convention (dotfiles in `$HOME`) + the **`ext4` on-disk format** (e2fsprogs `e2fsck`/`debugfs` as the oracle), the booted OS applying it under a real **libc** (`busybox`) shell on holospaces' own emulator | *no new artifact* — the shell base is the same published **BusyBox v1.37.0** riscv64 image as `CC-22` (`vv/artifacts/cc22/image/`, reused, not duplicated); the personalization is the operator's *own* content (an identity, a `.gitconfig`, editor settings, a `GH_TOKEN` secret), synthesized in the witness; reuses the `CC-14` kernel; notes in `cc23/SOURCE.txt` | `cc23-personalization.sh` → `tests/cc23_personalization.rs` + the `personalization` unit tests: a `Personalization` is a hologram `Realization` embedding the **operator identity** — the *same* settings/dotfiles/secrets under a *different* operator yield a *different* κ (scoped content, Laws L1/L3), and it round-trips through its canonical form; (1) the operator's dotfiles + entry `/init` are **injected into the assembled `ext4` rootfs** — `e2fsck` clean and `debugfs` reads `/root/.gitconfig` **and** `/init` back byte-identically; (2) **holospaces' own emulator applies the personalization** under a real libc (`busybox`) shell — `SECRET-PRESENT:GH_TOKEN` (the secret is in the OS environment, value *not* leaked) and `DOTFILE-PRESENT:.gitconfig` + its content (the operator's dotfile is in the home directory). The editor settings are carried in the κ-addressed personalization (round-trip); handing them to the running workbench is the `CC-13`/`CC-17` web path |
| `CC-13` VS Code workspace | the **real VS Code editor + terminal components** — Monaco editor + xterm.js (the libraries VS Code / Codespaces / Gitpod are built from), **imported by κ and verified by re-derivation through the substrate** (Law L5; CC-1 blake3), plus the Dev Container spec (`CC-4`) and the running OS terminal (`CC-9`/`CC-11`) | `vv/artifacts/cc13/vendor/` — the pinned `monaco-editor@0.52.2` (min/vs AMD core) + `@xterm/xterm@5.5.0` + `@xterm/addon-fit@0.10.0`, mirrored verbatim from npm; κ in `cc13/vendor.kappa`, sha256 in `cc13/vendor.sha256`, pins in `cc13/SOURCE.txt` | `cc13-vscode-workspace.sh` → `tests/cc13_vscode_workspace.rs` (the pinned components re-derive to their κ via the substrate's own `verify_kappa` — L5/CC-1; a forged byte is refused). **Live browser workspace:** `scripts/browser-manager-test.sh` (CI `browser` job) drives `crates/holospaces-web/web/workspace-test.mjs` (Playwright/Chromium): the workspace tab fetches Monaco + xterm.js from the gateway, **κ-verifies them through the browser peer before loading**, boots the holospace's devcontainer OS on the emulator, and presents it as the IDE — the xterm.js terminal drives the running OS (output matches the `CC-11`/QEMU oracle), the Monaco editor + file tree read/edit content by κ |
| `CC-35` AArch64 (A64) integer ISA | the **Arm Architecture Reference Manual** (ARM DDI 0487) for the A64 base instruction set + `PSTATE.NZCV`, with **`qemu-aarch64`** (linux-user) as the differential oracle — the AArch64 analogue of `CC-9.a` | `vv/artifacts/cc35/*.s` → `*.bin` — real, **toolchain-assembled** A64 self-checking batteries (`llvm-mc -triple=aarch64-linux-gnu` + `llvm-objcopy`, the external assembler — *not* hand-encoded; GNU `aarch64-linux-gnu-as` the equivalent fallback), each exercising one instruction group and writing `PASS\n`/exit 0 only when every Arm-ARM-defined result holds; `arith` (data-processing: MOVZ/MOVK, ADD/SUB imm+shifted+extended, the logical group, MUL/MADD/UMULH/SDIV, the shift/bitfield/EXTR group, CLZ/RBIT, CSEL), `memory` (the load/store family + LDP/STP + the extension variants), `control` (the branch family + NZCV: a CBNZ loop summing 1..=100=5050, BL/RET, TBNZ/TBZ/CBZ); position-independent so the *same* machine code runs on the core and under qemu-aarch64; pins + reproduce in `cc35/SOURCE.txt`, `sha256` in `cc35.sha256` | `cc35-aarch64-core.sh` → the `emulator::aarch64` unit tests (every A64 instruction group against its Arm-ARM-defined result) **and** `tests/cc35_aarch64.rs` (the core runs each real assembled battery to `PASS\n` + status 0); the **`qemu-aarch64` differential** builds the static `_start` ELFs and runs the same batteries, comparing stdout + status, re-derived live whenever qemu is present (the oracle is never stale) |
| `CC-36` a real arm64 Linux boots to userspace | the **Linux kernel** (a real, unmodified `arm64` OS — the most stringent A64 + privileged-architecture correctness test) over the **ARM `virt` platform**, with **`qemu-system-aarch64 -M virt`** as the differential oracle | `vv/artifacts/cc36/linux/Image.gz` — a real, reproducible **arm64 Linux 6.6** kernel (`torvalds/linux@v6.6`, `make ARCH=arm64 defconfig` + the initramfs splice + virtio/9p/ext4 options; fixed `KBUILD_BUILD_*`), built by `aarch64-linux-gnu-gcc`; a freestanding static PID-1 (`init.c`, raw `svc` syscalls, no libc) that prints `HOLOSPACES-LINUX-USERSPACE-OK` + the real `/proc/version` then reboots (→ PSCI `SYSTEM_OFF`); `kernel.config`, `expected-userspace.txt` (the qemu oracle), `linux.sha256`; provenance + the reproducible build in `cc36/linux/SOURCE.txt` | `cc36-aarch64-linux.sh` → `tests/cc36_aarch64.rs::the_emulator_boots_real_arm64_linux_to_userspace` (release): the privileged AArch64 system (`emulator::aarch64` with the EL0/EL1 exception model, VMSAv8-64 4 KiB-granule paging + TLB, the `virt` devices — GICv2 distributor/CPU interface, the CNTP/CNTV generic timer, a PL011 console, PSCI over SMC — and the SIMD load/store the kernel's FP context-switch needs) loads the kernel at `text_offset` with the holospaces-generated devicetree, and a **real `arm64` Linux boots to userspace**: it enables the MMU, takes timer interrupts, reaches `Run /init`, PID 1 prints its marker + the real `/proc/version`, and the machine powers off via PSCI — **byte-identical to `qemu-system-aarch64 -M virt`** on the same image (`expected-userspace.txt`, re-derived live whenever qemu is present, so the oracle is never stale). A real OS boot exercises the A64 base + system ISA, the EL0/EL1 trap architecture, VMSAv8-64 translation, the GIC + generic timer, and PSCI as one whole |
| `CC-37` arm64 devcontainer runs a stock linux-arm64 binary | the **Dev Container + OCI image specs** over an `arm64/linux` rootfs and a **stock, unmodified `linux-arm64`** binary (built by the upstream `aarch64-linux-gnu` toolchain) as the authority — no per-ISA porting — with **`qemu-system-aarch64 -M virt`** as the differential oracle | `vv/artifacts/cc37/linux/Image.gz` — a real arm64 Linux 6.6 kernel built like `CC-36` but with **no embedded initramfs** (it mounts a real `virtio-blk` root) + EXT4/virtio; `vv/artifacts/cc37/init.c` → `init` — a **stock `linux-arm64` toolchain binary** (`aarch64-linux-gnu-gcc -O2 -static -nostdlib`, the base A64 + system ISA) that runs a real computation + the `uname` syscall + reads `/proc/version`; `rootfs/busybox` + `layer.tar.gz` — a **stock glibc `linux-arm64` busybox 1.36.1** committed as the artifact whose full execution reaches the Advanced-SIMD frontier; pins + reproduce in `cc37/SOURCE.txt`, `cc37.sha256` | `cc37-aarch64-devcontainer.sh` → `tests/cc37_aarch64.rs`: the AArch64 system boots an `arm64` devcontainer from a **κ-disk `virtio-blk` rootfs** assembled by the in-crate Layer Assembler (`CC-7`) — serviced by the **shared** substrate device bus (`emulator::devbus`, the *same* κ-disk/9p/NAT servicing the RISC-V machine uses, GIC-routed; Law L4, one implementation) — and the stock `linux-arm64` binary runs as PID 1 (`CC37-DEVCONTAINER-UP` · `CC37-COMPUTE:500500` · `CC37-ARCH:aarch64` · the real `/proc/version`), **byte-identical to `qemu-system-aarch64 -M virt`** booting the same kernel + rootfs. The heavier glibc binaries (busybox, Node, `openvscode-server`) boot the same way and reach the Advanced-SIMD (NEON) frontier — ADR-020/`CC-34`'s open extension-host frontier, no riscv64 workaround |
| `CC-46` the shared device bus serves 9p + network + the bridge to a **real arm64 boot** | the **9P2000.L** + **OASIS VirtIO v1.2** specs and the `CC-15`/`CC-16`/`CC-33` authorities, run on the **AArch64** core at real-OS caliber (a real arm64 Linux boot), with x86-64 at device-level only (the `virtio-mmio` transport over the shared devbus, no kernel boot), with **`qemu-system-aarch64 -M virt`** + the **`10.0.2.0/24` user-mode (slirp) NAT** model as the differential oracle | `vv/artifacts/cc46/linux/Image.gz` + `linux/kernel.config` — a real arm64 Linux 6.6 kernel **identical to `CC-37`'s** (its config already enables `VIRTIO`/`VIRTIO_MMIO`/`VIRTIO_BLK`/`EXT4` + `VIRTIO_NET` + `NET_9P`/`NET_9P_VIRTIO`/`9P_FS` + `IP_PNP_DHCP` — exactly the devbus complement); `rootfs/busybox` + `layer.tar.gz` — a **stock glibc `linux-arm64` busybox 1.36.1** (the same as `CC-37`) whose stock applets the witness drives (`mount` for 9p, `wget` for the NAT outbound, `httpd` for the bridge listener); `init.sh` → `init` — the PID 1 that mounts the 9p workspace + round-trips a file, fetches over the NAT, and serves `httpd` on `:8080`; pins + reproduce in `cc46/SOURCE.txt`, `cc46.sha256` (extends `CC-37`'s recipe — only `/init` differs) | `vv/suites/cc46-devbus-arch-parity.sh` → `tests/cc46_realboot.rs::the_aarch64_core_serves_9p_net_and_bridge_to_a_real_arm64_boot` (release, `#[ignore]`d — run explicitly by the suite with `--ignored`): a **real arm64 Linux boots over the SHARED `emulator::devbus`** (the `virtio-blk` root, `virtio-9p` workspace, and `virtio-net` device all advertised in the devicetree, GIC-routed; Law L4, one implementation) and a real guest userspace **mounts the 9p workspace through its VFS** (reads the seeded file `from-holospaces-9p-share-OK`, writes `from-guest.txt` holospaces reads back — `CC-15`), **opens an outbound TCP/HTTP flow over `virtio-net` through the userspace NAT** to a real host server (`HELLO-FROM-HOST-SERVER` — `CC-16`), and a **real guest server (`busybox httpd`) is reached from the host over the in-process bridge** (`dial_guest` → `guest_send`/`guest_recv` → `HELLO-FROM-GUEST-SERVER` — `CC-33`) — `CC-15`/`CC-16`/`CC-33` re-run on the AArch64 core at the same real-boot caliber. x86-64 is covered at device level only by `tests/cc46_devbus_parity.rs` (both cores' `virtio-mmio` transports → the shared devbus, fast regression coverage); there is no x86-64 kernel-boot witness in this row |
| `CC-44` a real amd64 (x86-64) Linux boots to userspace | the **Intel® 64 and IA-32 Architectures SDM** — the *published x86-64 architecture specification* the core is **built to** (Vol 3A: long-mode paging, the local APIC + APIC timer, the IDT and interrupt/exception delivery, the 8259-via-LINT0 virtual-wire mode, `IA32_TSC`; Vol 2: instruction semantics) — imported + pinned in `vv/artifacts/intel-sdm/` (`SOURCE.txt`, `intel-sdm.sha256`, fetch-and-verify `fetch.sh`); with the **Linux kernel** (a real, unmodified x86-64 OS — the most stringent AMD64 + privileged-architecture correctness test for the third ISA) booting to userspace on the holospaces x86-64 core as the integration witness and **`qemu-system-x86_64`** as the differential oracle | `vv/artifacts/cc44/linux/vmlinux.gz` — a real, reproducible **x86-64 Linux 6.6** kernel (`torvalds/linux@v6.6`, `make ARCH=x86_64 x86_64_defconfig` + the initramfs splice + virtio-mmio/virtio/9p/ext4/8250-console options; fixed `KBUILD_BUILD_*`), the *uncompressed* `vmlinux` ELF the core loads (`PT_LOAD` segments at their physical addresses, entered at `startup_64` via the 64-bit boot protocol — no in-guest decompressor); `bzImage` — the standard self-decompressing kernel from the same source/config, the image `qemu-system-x86_64 -kernel` boots for the differential; a freestanding static PID-1 (`init.c`, raw `syscall`, no libc) that prints `HOLOSPACES-LINUX-USERSPACE-OK` + the real `/proc/version` then powers off (LINUX_REBOOT_CMD_POWER_OFF → `native_machine_halt` → `hlt`); `kernel.config`, `expected-userspace.txt` (the qemu oracle), `linux.sha256`; provenance + the reproducible build in `cc44/linux/SOURCE.txt` | `cc44-x64-linux.sh` → `tests/cc44_x64_boot.rs::an_amd64_linux_kernel_boots_to_userspace` (release, `#[ignore]`d — run explicitly by the suite with `--ignored`): the x86-64 system core (`emulator::x64` with the 64-bit Linux boot protocol — `boot_params`/the zero page + e820, the GDT, long-mode paging — an IDT + a minimal interrupt controller (8259 PIC / 8254 PIT / local APIC) so the timer and `virtio` IRQs vector, the `virtio-mmio` κ-disk over the **shared** `emulator::devbus`, and the instruction tail the boot path hits) loads the committed `vmlinux` and a **real x86-64 Linux boots to userspace**: it installs its page tables, takes timer interrupts, reaches `Run /init`, PID 1 prints its marker + the real `/proc/version`, and the machine powers off — **byte-identical to `qemu-system-x86_64`** booting the same kernel (`expected-userspace.txt`, re-derived live whenever qemu is present, so the oracle is never stale). The third ISA realization of `CC-36` (aarch64) / `CC-9` (riscv64) |
| `CC-45` an arbitrary amd64 devcontainer runs on x86-64 (build-capable) | the **Dev Container + OCI image specs** over an `amd64/linux` rootfs and a **stock, unmodified `linux-amd64`** binary (built by the upstream busybox + native `x86_64` toolchain) as the authority — no per-ISA porting (Law L4) — with **`qemu-system-x86_64`** as the differential oracle; plus the **build-capable disk** invariant: boot is **O(content), not O(disk)** (the occupancy-index path), so a multi-GiB disk boots promptly | `vv/artifacts/cc45/linux/vmlinux.gz` + `bzImage` — a real, reproducible **x86-64 Linux 6.6** kernel (`torvalds/linux@v6.6`, `make ARCH=x86_64 x86_64_defconfig`) built like `CC-44` but with **no embedded initramfs** (`INITRAMFS_SOURCE=""`, `BLK_DEV_INITRD` off) so it mounts a real `virtio-blk` root, + `VIRTIO_MMIO`/`VIRTIO_MMIO_CMDLINE_DEVICES`/`VIRTIO_BLK`/`EXT4`/virtio-net/9p/8250-console, 4-level paging (`X86_5LEVEL` off); the κ-disk is discovered via `virtio_mmio.device=0x200@0xd0000000:11` (x86 has no DTB); `rootfs/busybox` + `layer.tar.gz` — a **stock glibc `linux-amd64` busybox 1.36.1** (`make ARCH=x86_64 defconfig` + `CONFIG_STATIC=y`, stripped), the OCI layer the in-crate Layer Assembler (`CC-7`) overlays; `init.sh` → `/init` — the busybox-shell PID 1 (`uname -m` → `x86_64`, a shell compute `sum 1..=1000 == 500500`, `head /proc/version`, then `poweroff`); `build/tcc` + `build/hello.c` — the **in-guest toolchain** (a static `linux-amd64` TinyCC, pinned mob `a338258d`, + a freestanding source) the build-capable witness compiles + runs inside the guest; `image/` — a real **`linux/amd64` OCI image-layout** (oci-layout + index.json + blobs by digest) wrapping the busybox layer, ingested through the production `oci::ingest_image` path; pins + reproduce in `cc45/SOURCE.txt`, `cc45.sha256` | `cc45-x64-devcontainer.sh` → `tests/cc44_x64_boot.rs` (the witnesses below are `#[ignore]`d release tests, run explicitly by the suite with `--ignored`): (1) `an_amd64_devcontainer_runs_a_stock_linux_amd64_binary` — the x86-64 core boots an `amd64` devcontainer from a **κ-disk `virtio-blk` rootfs** assembled by the Layer Assembler over the **shared** `emulator::devbus` (the *same* κ-disk every ISA uses; Law L4), and the stock `linux-amd64` busybox runs as PID 1 (`CC45-DEVCONTAINER-UP` · `CC45-COMPUTE:500500` · `CC45-ARCH:x86_64` · the real `/proc/version`), **byte-identical to `qemu-system-x86_64`** booting the same kernel + rootfs; (2) `an_amd64_devcontainer_boots_paged_from_a_kappa_store` — the same boot with the κ-disk **streamed** sector-by-sector (the deployed `X64Workspace`/OPFS path); (3) `an_amd64_linux_boots_from_an_occupancy_indexed_build_capable_disk` + `occupancy_index_pages_only_content_for_a_multi_gib_disk` + `an_amd64_devcontainer_boots_occupancy_streamed_from_a_large_disk` — an **≥ 8 GiB** declared disk boots **O(content)** (the disk-size ceiling removed: the κ-disk index is a sparse `BTreeMap` paging only occupied sectors, streamed from the deployed OPFS medium); (4) the **full Dev Container / OCI contract** — `…_multilayer_image_overlay_runs` (OCI whiteout/override/add), `…_dockerfile_build_runs_on_x64` (`CC-26`), `…_features_and_lifecycle_run_on_x64` (`CC-25`/`CC-22`), `holospaces_parses_its_own_unmodified_devcontainer_config` (dogfood); (5) **build-in-guest** — `an_amd64_devcontainer_builds_a_program_in_guest`: a static `linux-amd64` TinyCC **compiles a program inside the guest and the guest runs the binary it built** (`CC45-TCC-EXIT:0` → `CC45-BUILT-IN-GUEST:42`), the decisive build-capable proof; (6) **the shared 9p workspace** — `the_amd64_devcontainer_shares_the_9p_workspace_with_the_editor`: a real amd64 Linux boots with the deployed init + BOTH `virtio-mmio` slots declared, **mounts the `hsworkspace` share, reads a host-written file in-guest and writes a file the editor observes** (both directions over `virtio-9p`, plus the nested-path tree the workbench `FileSystemProvider` drives) — the parity that makes an x86-64 holospace a *usable* devcontainer (editor/tasks/search bind to the same content); (7) **arbitrary real OCI image** — `an_arbitrary_real_amd64_oci_image_boots_on_x64`: a real `linux/amd64` OCI image-layout is ingested through the **production** `oci::ingest_image` path (index → amd64 manifest → config + layers, every blob verified by re-derivation, Law L5) and boots on the x86-64 core — the Codespaces `image:` → pull → boot resolution end to end; (8) **the dogfood (this repo's OWN devcontainer)** — `vv/heavy/cc45-dogfood-devcontainer.sh` builds this repo's unmodified `.devcontainer` (Ubuntu 24.04 + the toolchain + ghcr features) with the Dev Container CLI, exports its rootfs, and `tests/cc45_dogfood.rs::holospaces_builds_in_its_own_real_devcontainer` boots it on the x86-64 core where the **real gcc 13.3** (cc1 → as → ld over glibc 2.39 + ld.so) compiles a program in-guest and the guest runs the binary it built (`DOGFOOD-GCC-BUILT:42`). This is an **on-demand HEAVY suite** (`vv/heavy/`, not the per-push gate): it needs docker + ~24 GiB RAM (a full Ubuntu+gcc boot's κ-disk + COW state), which OOMs the free 16 GiB CI runner — so the per-push gate proves build-capability with the fast in-guest witnesses (5)/(6); this is the full-fat dogfood, run on a machine with the RAM. No committed fixture, no stub, no skip. Running real glibc/gcc surfaced + fixed genuine emulator defects (CPUID EBX delivery, CMPXCHG8B/16B register, CLFLUSH line size, XORPS/PREFETCHW + the SSE2 scalar/packed float + packed-int families). The amd64 realization of `CC-37` (aarch64) on the build-capable disk |
| `CC-51` Source Control (Git) over the holospace's own workspace | the **VS Code SourceControl (`scm`) API** + the **Git on-disk object** (`gitformat-pack`) and **smart-http pack-protocol** formats — the authority a commit's bytes re-derive against (Law L5) — with **isomorphic-git** (a faithful, spec-conformant pure-JS Git implementation; its commit/tree/blob objects ARE canonical Git objects, readable by stock `git`) as the engine, and a real **`git http-backend`** bare repo as the **independent push oracle** (`git log` on the remote) | `crates/holospaces-web/web/builtin-extensions/holospace-scm/vendor/isomorphic-git/{index.umd.min.js,http-web.umd.js}` — the vendored Git engine, **isomorphic-git@1.27.1** (MIT) UMD bundles, the only self-contained distributables (the ESM `module` build has bare imports); pinned + reproduced in `vendor/isomorphic-git/SOURCE.txt` + `SHA256SUMS`, and the extension **re-derives each bundle to its pinned sha256 before it runs** (Law L5, the import boundary). The host witness reuses the committed **`CC-18`** busybox image + **`CC-14`** kernel (no new boot artifact); the deployed witness's remote is a **local bare repo** served same-origin over `git http-backend` (hermetic — no third party) | `vv/suites/cc51-scm-git.sh` → **(host)** `tests/cc51_nested_workspace.rs::the_host_and_os_share_a_nested_workspace_tree_over_virtio_9p` (release, `#[ignore]`d — a real busybox boot): the host's nested-path 9p API writes `.git/objects/ab/cdef` (creating intermediate **directory inodes**) which the booted OS **reads back byte-identically over `virtio-9p`** (host → guest), and the OS writes `src/deep/from-guest.txt` which the host reads back through `resolve_path` (guest → host) — one nested content, both directions (`CC-15` parity with the guest's `Twalk`/`Tcreate`, Law L1); **(deployed)** `crates/holospaces-web/web/scm-git-test.mjs` (Chromium/Playwright, the real workbench): the **`scm` count 0 → a registered `holospace-git` SourceControl provider** (its status-bar marker published only after `activate()`+`refresh()`); after **Initialize Repository** the workspace's files appear as **working-tree changes over 9p** (real status); a **commit through the SCM input** is a real Git commit whose oid **re-derives** (the witness + the extension independently recompute `sha1("commit "+len+"\0"+content)` == the engine's oid) with **HEAD pointing at it** (Law L5, the object-format authority); and **Push** over the smart-http pack-protocol **lands in the real `git http-backend` bare repo** — the independent oracle `git -C <bare> log` shows the **same sha + message**. The provider runs as **native exec on the browser peer** (the `CC-48` discipline) over the holospace's OWN `virtio-9p` workspace — the same `.git` the guest's git reads — no server outside the holospace (Law L4) |
| `CC-52` Workspace search & replace (find-in-files) over the holospace's own workspace | the VS Code **`FileSearchProvider` / `TextSearchProvider`** search-provider API (the `fileSearchProvider`/`textSearchProvider` proposals — a *builtin* keeps its self-declared `enabledApiProposals`, enabled per-extension via `productConfiguration.extensionEnabledApiProposals`), with the workspace's **actual file content** as the authority a match re-derives against (a reported range slices to exactly the matched text, at its line/column — Law L5); `.gitignore` semantics from the spec-conformant **`ignore`** matcher | `crates/holospaces-web/web/builtin-extensions/holospace-search/vendor/ignore/index.cjs` — the vendored `.gitignore` engine, **ignore@5.3.1** (MIT), pinned + reproduced in `vendor/ignore/SOURCE.txt` + `SHA256SUMS`; the extension **re-derives it to its pinned sha256 before it runs** (Law L5). No bootable artifact — the witness searches the holospace's boot-seeded `main.rs` over 9p | `vv/suites/cc52-search.sh` → **(core)** `builtin-extensions/holospace-search/search-core.test.cjs` (Node, deterministic, 30 checks): VS Code glob include/exclude (`**`, `*`, `?`, `{a,b}`, `[...]`), `.gitignore` via the vendored `ignore` (negation, dir-only, anchoring), the **streaming + cancelling + excludes-pruning** tree walk with `maxResults`/`limitHit`, and literal/regex/case/word/multiline text matching whose reported `(line,col)` **slices to exactly the matched text** (Law L5); **(deployed)** `crates/holospaces-web/web/search-test.mjs` (Chromium, the real workbench): the `holospace-search` File + Text providers are **live** (`HOLOSPACE-SEARCH-LIVE`) — the workbench has *no* fallback search for a virtual scheme, so this is what makes find-in-files work; a query for `greet` returns the **expected 3 matches in `main.rs`** and the Search view **renders the result rows**; and a **replace-all** **lands its edit over 9p** — with the editors closed (so the provider re-reads the *settled* file, not a cached editor model), a re-search finds `greet` **0** times: the workspace replace, applied through the holospace FileSystemProvider's `writeFile`, persisted to the workspace the guest shares (Law L1). Native browser-peer exec (the `CC-48` discipline) over the holospace's OWN `virtio-9p` workspace (`CC-15`) — no server outside the holospace (Law L4) |
| `CC-53` Tasks runner (`tasks.json`) — tasks run in the devcontainer over the holospace's own primitives | the VS Code **Task provider API** (`registerTaskProvider` + `CustomExecution` + `Pseudoterminal`, stable — no proposals; a `contributes.taskDefinitions` type) + the **`tasks.json`** schema, with the task command's **real execution in the guest** as the authority — its exit code IS the agent's `echo $?`, its output IS the bytes the guest wrote, and a problem-matcher line becomes a diagnostic at its file/line. The web workbench **disables** shell/process task execution in a virtual workspace (the `virtualWorkspace` context gates them off), so a `CustomExecution` provider is the only path tasks.json runs | no vendored/bootable artifact: the **guest task-runner agent** is a `/bin/sh` loop compiled into the devcontainer `/init` (`crates/holospaces/src/machine.rs`, `DEVCONTAINER_INIT` + `REAL_IMAGE_INIT`) that watches `/workspace/.hs-tasks/` over the shared `virtio-9p` workspace, runs each task in its **own background subshell** (tasks run *concurrently* — a long-running watch task blocks nothing) and honours a `<id>.kill` sentinel (terminating a task genuinely kills the guest process); the witness reuses the committed **`CC-18`** busybox image + **`CC-16`** kernel, and the workbench's seeded `.vscode/tasks.json` | `vv/suites/cc53-tasks.sh` → **(core)** `builtin-extensions/holospace-tasks/tasks-core.test.cjs` (Node, deterministic, 27 checks): the `tasks.json` (JSONC, string-aware comment/trailing-comma stripping) parse, the **devcontainer.json lifecycle-command surfacing** (string / argv / named-object forms each become a re-runnable task; the host-side `initializeCommand` is excluded — ADR-015), the shell-command build (cwd / env / single-quote arg-escaping), and the file-exec request/stream/exit protocol (paths + the `echo $?` exit parse) end-to-end against a simulated agent; **(deployed)** `crates/holospaces-web/web/tasks-test.mjs` (Chromium, the real workbench): the `holospace-tasks` TaskProvider is **live** (`registerTaskProvider` 0 → a provider), the default **build** task **RUNS in the devcontainer** — the guest agent runs its command and its output + **non-zero exit status** (`code 2`, read from the agent's `<id>.exit` over 9p) surface through the `CustomExecution` Pseudoterminal — and the task's **problem matcher** turns its `main.rs:2:5: warning: TODO found here` line into a **diagnostic** in the Problems panel (VS Code feeds the pty output to the same problem collector as a shell task); the devcontainer's **lifecycle commands** (`CC-22`) surface as re-runnable tasks — the seeded `devcontainer.json`'s `postCreateCommand` runs in the guest and exits 0 — and a **background/watch task runs without blocking**: while the long-running `watch` task keeps streaming from the guest, the build task runs to its exit (concurrent guest execution, the UI + task system stay usable). Over the holospace's OWN virtio-9p workspace (`CC-15`) + the devcontainer shell (`CC-11`) — no server outside the holospace (Law L4) |

### `CC-9` (witnessed) — the system emulator boots a real operating system

CC-9's end state — *a real operating system boots and runs on the emulator* — is **reached and witnessed**: a real, unmodified Linux 6.6 kernel boots to userspace on the emulator core, and its console output matches `qemu-system-riscv64` (the reference RISC-V implementation) byte-for-byte on the same image. The authorities are the **RISC-V ISA** (the official `riscv-tests` suite, exactly as `CC-5` is grown against the WebAssembly spec suite), the **Linux kernel itself** (a real OS boot is the most stringent correctness test of a machine), and **QEMU** (the differential oracle). The `vv/suites/cc9-emulator` suite is live.

| Step | Authority | Source / pin | Verified by |
|---|---|---|---|
| `CC-9.a` emulator ISA + privileged conformance | the **official RISC-V `riscv-tests` conformance suite** — the canonical authority real hardware and QEMU are validated against (<https://github.com/riscv-software-src/riscv-tests>); plus a small `llvm-mc`-assembled battery for the host-syscall path | `vv/artifacts/cc9/riscv-tests/*.bin` — the machine-mode `-p` tests `rv64ui`+`rv64um`+`rv64ua`+`rv64uc` (full) plus the **full** `rv64mi`/`rv64si` machine-/supervisor-mode suites (the WARL/read-only CSRs, the debug-trigger absence, misaligned access, the FP-disabled trap, and the composite vectored-interrupt/S-mode/paging test with `mstatus.TVM`/`TSR` semantics — no exclusions), built by `riscv64-unknown-elf-gcc` (pins + command + `tohost` manifest in `riscv-tests/SOURCE.txt`); plus `vv/artifacts/cc9/*.bin` (`llvm-mc`, `expected.txt`) | `tests/cc9_emulator.rs`: the `emulator::Emulator` (RV64GC = IMAFDC + Zicsr + machine/supervisor traps + Sv39/Sv48/Sv57 paging + CLINT timer interrupts) **passes 134 official riscv-tests** (incl. rv64uf/rv64ud floating point — correctly-rounded with the IEEE-754 flags and all rounding modes) — each installs `mtvec`, drops to a lower mode via `mret`/`sret`, runs its self-checking cases, and signals pass/fail through the HTIF `tohost` channel, so the privileged trap architecture (`ecall`/`ebreak` exceptions, delegation, `sret`, Sv39/Sv48/Sv57 supervisor paging) is exercised as well as the base ISA. Separate witnesses confirm a **CLINT timer interrupt** fires into the handler (`tint.S`/`.bin`) and that the emulator-as-SEE services **SBI** console + system-reset calls from a supervisor kernel (`sbi.S`/`.bin`) — so a real S-mode OS boots over it. The κ snapshot verifies by re-derivation (L5) and is reproducible across peers (L1), read off a `CC-7` κ-disk |
| `CC-9.b` emulator on the substrate | the **hologram `ContainerRuntime` contract** + the **WebAssembly spec / host-ABI surface** (`CC-6`) — the real Wasmtime engine as the execution oracle | `vv/artifacts/cc9/emulator.wasm` — the emulator core compiled to a hologram container codemodule (crate `holospaces-emulator`; imports only `hologram.storage_put`/`storage_get`, exports the container ABI); built by `scripts/build-emulator.sh`, `sha256` in `bins.sha256`, command in `SOURCE.txt` | `tests/cc9_emulator.rs`: the codemodule validates against the execution-surface contract (`surface::validate_userland`), then is **spawned on the real Wasmtime `Runtime`** with a guest image as its initial state; it runs the real RISC-V program and emits the ISA-defined result into the substrate via the host ABI (content-addressed — the result κ is the guest's deterministic output); the container κ snapshot is reproducible. The emulator is κ-addressed Wasm over the host ABI, not a parallel medium (Law L4) |

| `CC-9.c` a real Linux kernel boots to userspace | the **Linux kernel** (a real, unmodified RV64GC OS — the most stringent ISA correctness test) and **`qemu-system-riscv64`** (the reference RISC-V machine, the differential oracle) | `vv/artifacts/cc9/linux/Image.gz` — the pinned Linux **6.6** kernel (`torvalds/linux@v6.6`, trimmed RV64GC config), built reproducibly (fixed `KBUILD_BUILD_TIMESTAMP`/`USER`/`HOST`); a freestanding static PID-1 (`init.c`) + initramfs + device tree; provenance, the reproducible build script, and the QEMU-captured `expected-userspace.txt` in `linux/SOURCE.txt`; `sha256` in `linux/linux.sha256` | `tests/cc9_emulator.rs::the_emulator_boots_real_linux_to_userspace`: the emulator is the SBI firmware (`boot_kernel`), loads the kernel + DTB, and runs until PID 1 powers off via the SBI system-reset call. The kernel reaches `Run /init`, PID 1 (a real static ELF) prints its marker and the real `/proc/version`, and that userspace output equals `expected-userspace.txt` — captured from `qemu-system-riscv64` booting the same image, and re-derived live by the suite whenever QEMU is present (the oracle is never stale). A real OS boot exercises the base ISA, the M/A/F/D/C extensions, Sv39/Sv48/Sv57 paging, the machine/supervisor trap architecture, the CLINT timer, and the SBI as one whole |

| `CC-9.d` a real OS boots **on the substrate** | the **hologram `ContainerRuntime` contract** + the emulator-core boot (`CC-9.c`) as the oracle | the `emulator.wasm` codemodule (`CC-9.b`) + the pinned Linux kernel (`CC-9.c`) | `tests/cc9_emulator.rs::the_codemodule_boots_real_linux_on_the_substrate`: the host hands the container a small **boot descriptor** as its initial state (`b"HGOS"` + the kernel-image κ + the device-tree κ); the codemodule reads the kernel and DTB back out of the substrate by κ with `storage_get` (content, not a located file — Laws L1/L4), becomes the SBI firmware, and **boots Linux to userspace on the real Wasmtime `Runtime`**, emitting `[exit_code][console]` via `storage_put`. That record is **byte-identical to the one the emulator core produces** (`CC-9.c`, which matches QEMU), so its content address is the same κ — and a second boot yields the same container κ snapshot (the OS boot on the substrate is deterministic, Law L1). ADR-009 fully realized: an arbitrary OS boots *on the substrate*, its image delivered as content and its result content-addressed |

The OS boots both on the emulator core (against the QEMU oracle, `CC-9.c`) and *through* the codemodule on the hologram runtime (`CC-9.d`); the operator drives it interactively as a workspace projection (`CC-11`). Every implemented surface is witnessed against an external authority.
